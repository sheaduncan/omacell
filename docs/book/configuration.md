<!-- Generated by `scripts/generate-docs.py`; do not edit. -->

# Configuration

Configuration is layered from package defaults, user TOML, workbook settings, environment, and CLI overrides. Unknown keys are rejected. `schema = 1` is required. The table below is generated from `docs/schemas/config.schema.json`.

| Key | Type | Schema default | Description |
|---|---|---|---|
| `ai.agent.autopilot_max_ops` | uint32 | — | Cap. |
| `ai.agent.autopilot_scope` | `sheet` / `range` / `workbook` | — | `sheet` / `range` / `workbook`. |
| `ai.agent.diagnose_offers` | boolean | — | Diagnose offers. |
| `ai.agent.panel` | boolean | — | Agent panel. |
| `ai.agent.review` | `always` / `autopilot_opt_in` | — | `always` / `autopilot_opt_in`. |
| `ai.agent.skills_dir` | string | — | Skills directory. |
| `ai.completion.debounce` | uint32 | — | Debounce ms. |
| `ai.completion.mode` | `auto` / `on` / `off` | — | `auto` / `on` / `off`. |
| `ai.enabled` | boolean | — | Master switch. |
| `ai.functions.auto` | boolean | — | Re-query on input change. |
| `ai.functions.batch_size` | uint32 | — | Batch size. |
| `ai.functions.keep_stale` | boolean | — | Keep stale values. |
| `ai.functions.max_cells_per_recalc` | uint32 | — | Confirm above this many cells. |
| `ai.functions.max_requests_per_minute` | uint32 | — | Rate limit. |
| `ai.functions.refresh_on_full_recalc` | boolean | — | Refresh on full recalc. |
| `ai.functions.xlsx_export` | `formulas` / `values` | — | `formulas` / `values`. |
| `ai.models.agent` | string | `""` | Agent slot. |
| `ai.models.default` | string | — | Default slot. |
| `ai.models.fast` | string | — | Fast slot. |
| `ai.models.strong` | string | `""` | Strong slot. |
| `ai.models.vision` | string | `""` | Vision slot. |
| `ai.privacy.local_full` | boolean | — | Local providers send full. |
| `ai.privacy.log_content` | boolean | — | Log payloads. |
| `ai.privacy.send` | `schema` / `sample` / `full` | — | `schema` / `sample` / `full`. |
| `ai.privacy.suggest_redaction` | boolean | — | Suggest redaction. |
| `ai.providers` | object | `{}` | Named providers. |
| `ai.status_segment` | boolean | — | Status-line segment. |
| `appearance.animation` | `system` / `on` / `off` | — | `system` / `on` / `off`. |
| `appearance.cell_font` | string | — | Cell fontconfig alias. |
| `appearance.cell_font_size` | number | — | Cell size in pt. |
| `appearance.cell_padding` | uint32 | — | Padding px at 1×. |
| `appearance.column_width` | number | — | Default column width in characters. |
| `appearance.corner_style` | `system` / `rounded` / `sharp` | — | `system` / `rounded` / `sharp`. |
| `appearance.cursor_style` | `outline` / `block` / `underline` | — | `outline` / `block` / `underline`. |
| `appearance.enforce_contrast` | boolean | — | WCAG nudge. |
| `appearance.grid_line_style` | `solid` / `dotted` / `none` | — | `solid` / `dotted` / `none`. |
| `appearance.grid_lines` | boolean | — | Draw grid lines. |
| `appearance.row_height` | object | — | `"auto"` or pt. |
| `appearance.selection_style` | `fill` / `outline` | — | `fill` / `outline`. |
| `appearance.sheet_tabs_position` | `top` / `bottom` | — | `top` / `bottom`. |
| `appearance.show_formula_bar` | boolean | — | Formula bar visible. |
| `appearance.show_sheet_tabs` | boolean | — | Sheet tabs visible. |
| `appearance.show_status_line` | boolean | — | Status line visible. |
| `appearance.ui_font` | string | — | UI fontconfig alias. |
| `appearance.ui_font_size` | object | — | `"system"` or pt. |
| `appearance.zebra_rows` | boolean | — | Alternate row fill. |
| `behavior.autocomplete` | boolean | — | Formula autocomplete. |
| `behavior.autocorrect` | boolean | — | Autocorrect. |
| `behavior.date_system` | `1900` / `1904` | — | 1900 or 1904. |
| `behavior.default_sheets` | uint32 | — | New-workbook sheet count. |
| `behavior.enter_moves` | `down` / `right` / `none` | — | `down` / `right` / `none`. |
| `behavior.fill_prompt` | boolean | — | Fill-options prompt. |
| `behavior.formula_hints` | boolean | — | Formula hints. |
| `behavior.precision_as_displayed` | boolean | — | Precision as displayed. |
| `behavior.reference_style` | `A1` / `R1C1` | — | `A1` / `R1C1`. |
| `behavior.smart_paste` | boolean | — | Detect delimited paste. |
| `calc.iterative` | boolean | — | Iterative calc. |
| `calc.max_change` | number | — | Max change. |
| `calc.max_iterations` | uint32 | — | Max iterations. |
| `calc.mode` | `automatic` / `automatic_except_tables` / `manual` | — | `automatic` / `automatic_except_tables` / `manual`. |
| `calc.threads` | object | — | `"auto"` or a thread count. |
| `calc.volatile_on_open` | boolean | — | Volatiles on open. |
| `charts.default_type` | string | — | Default chart type. |
| `charts.font` | string | — | `ui` or a font alias. |
| `charts.line_width` | uint32 | — | Line width. |
| `charts.palette` | string | — | `theme` or a named palette. |
| `config.debounce_ms` | uint64 | `50` | Debounce milliseconds. |
| `config.live_reload` | boolean | `true` | Watch user files. |
| `files.autosave_interval` | uint64 | — | Autosave seconds; 0 disables. |
| `files.csv.delimiter` | string | — | `auto` or a delimiter. |
| `files.csv.encoding` | string | — | `auto` or an encoding. |
| `files.csv.type_inference` | `conservative` / `aggressive` / `none` | — | `conservative` / `aggressive` / `none`. |
| `files.default_format` | `xlsx` / `omc` | — | `xlsx` / `omc`. |
| `files.follow_external_links` | boolean | — | Follow external links. |
| `files.keep_backups` | uint32 | — | Numbered backups. |
| `files.xlsx.preserve_unknown_parts` | boolean | — | Preserve L3 parts. |
| `integrations.libreoffice_fallback` | boolean | — | Deprecated compatibility key; `.xls` import is native. |
| `integrations.menu_entries` | boolean | — | Offer menu rows. |
| `integrations.notifications` | `all` / `recovery_only` / `off` | — | `all` / `recovery_only` / `off`. |
| `integrations.ocr_paste` | boolean | — | OCR paste. |
| `integrations.omarchy` | `auto` / `on` / `off` | — | `auto` / `on` / `off`. |
| `ipc.max_frame_bytes` | uint64 | — | Maximum JSON-line bytes including the trailing newline. |
| `keys.file` | string | — | Keymap file name under the config dir or package defaults. |
| `layout.compact_below_width` | uint32 | — | Compact-chrome width. |
| `layout.formula_bar_lines` | uint32 | — | Formula bar rows. |
| `layout.menu_bar` | boolean | — | Menu bar. |
| `layout.panel_side` | `right` / `left` / `bottom` | — | `right` / `left` / `bottom`. |
| `layout.panel_width` | uint32 | — | Panel width px. |
| `layout.status_line` | array | — | Status-line segments. |
| `locale.currency` | string | — | Currency or `system`. |
| `locale.date_format` | string | — | Date format or `system`. |
| `locale.decimal_separator` | string | — | Decimal separator or `system`. |
| `locale.first_weekday` | string | — | First weekday or `system`. |
| `locale.language` | string | — | Language tag or `system`. |
| `locale.list_separator` | string | — | List separator or `system`. |
| `locale.localized_function_names` | boolean | — | Localized function names. |
| `locale.thousands_separator` | string | — | Thousands separator or `system`. |
| `network.allow_functions` | array | — | Function allowlist. |
| `network.enabled` | boolean | — | Off by default. |
| `network.proxy` | string | — | Proxy URL. |
| `schema` | `1` | — | Schema version. |
| `scripting.embedded_scripts` | `sandbox` / `ask` / `deny` | — | `sandbox` / `ask` / `deny`. |
| `scripting.enabled` | boolean | — | Lua enabled. |
| `scripting.trusted_dirs` | array | — | Trusted directories. |
| `session.recent_files` | uint32 | — | Recent-file count. |
| `session.restore` | boolean | — | Restore windows. |
| `session.workspace_binding` | boolean | — | Remember Hyprland workspace. |
| `tui.graphics` | `auto` / `sixel` / `kitty` / `off` | — | `auto` / `sixel` / `kitty` / `off`. |
| `tui.mouse` | boolean | — | Mouse. |
| `tui.truecolor` | `auto` / `on` / `off` | — | `auto` / `on` / `off`. |
| `tui.unicode_borders` | boolean | — | Unicode box drawing. |

Package values are shown in `/usr/share/omacell/default/config.toml`; schema defaults above describe optional object defaults and constraints, not a second precedence layer. Use `omacell config show`, `diff`, and `validate` to inspect the effective configuration.

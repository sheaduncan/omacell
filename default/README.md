# Shipped defaults

Copied into `/usr/share/omacell/default/` by the package (WP-28). User overrides
live in `~/.config/omacell/` and are never overwritten by updates.

| File | Filled by |
|---|---|
| `config.toml` | WP-12 |
| `keys/classic.toml`, `keys/modal.toml` | WP-14 |
| `themed/omacell.toml.tpl` | WP-12 |
| `ai/prompts/` | WP-22, WP-23 |
| `agents/skills/omacell/SKILL.md` | WP-21 |

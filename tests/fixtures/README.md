# Test fixtures

Shared non-corpus fixtures. AI provider recordings live in `tests/fixtures/ai/`
(WP-22). Never commit third-party `.xlsx` files without permission; generate
them with `scripts/corpus-gen/` (WP-09).

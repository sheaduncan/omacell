# Corpus — Excel error values

Display strings and `ERROR.TYPE` codes for WP-01. Codes 1–8 follow
Microsoft’s ERROR.TYPE documentation; newer errors are recorded as empty
`error_type` (Excel returns `#N/A`).

# Audit corpora (WP-19)

`checks.tsv` lists every deterministic finding the seeded workbook in
`crates/core/tests/audit.rs` must emit. `explain.tsv` lists error-explanation
needles. Rows cite the documented behaviour they encode in the `note` column.

# Corpus — omc

UTF-8 `.omc` fixtures for WP-11 (Appendix E sketch and round-trip).

- `sketch.omc` — Appendix E example (comments, names, styles, cf, validation).
- Round-trip coverage of `.xlsx` lives in `crates/io/tests/omc_roundtrip.rs`
  (opens `tests/corpus/xlsx/*.xlsx`).

# Keep rustc/lld artifacts on the /home disk. /tmp is tmpfs; a full
# tmpfs makes rust-lld SIGBUS on mmap'd linker output.
mkdir -p "${HOME}/.cache/omacell/target"
export CARGO_TARGET_DIR="${HOME}/.cache/omacell/target"

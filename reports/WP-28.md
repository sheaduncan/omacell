# Report — WP-28: Packaging, documentation, Omarchy CI, hardening, accessibility, i18n scaffolding, release

## Plan (written before coding)

- Files/modules to create or complete:
  - Arch source and binary packaging under `packaging/`, including install hooks,
    desktop/MIME metadata, hicolor application and MIME icons, and a container
    smoke harness.
  - An mdBook manual under `docs/book/`, generated CLI/config/Lua references,
    generation and drift scripts, parser-limit documentation, privacy/AI and
    Omarchy integration chapters.
  - Packaging, Omarchy-channel, fixed-host performance, nightly fuzz, and tag
    release workflows under `.github/workflows/`, plus committed performance
    budgets and validation scripts.
  - Accessibility and keyboard walkthrough coverage in `crates/gui/tests/`, an
    `en-US` Fluent resource plus extraction/drift lint, explicit reduced-motion
    handling, split-pane painting, font-family/fallback cache coverage, and
    clickable status segments.
  - Print-title band fidelity, printer chooser/panel coverage, the documented
    PDF font contract, release changelog, and `scripts/rename.sh`.
- Interfaces to expose (types, commands, schemas, CLI):
  - Installed paths and package assets become the public distribution surface;
    generated completions/man/manual/skill/default/prompt files must match the
    binary and committed schemas.
  - Reuse the frozen `file.print {path?, printer?}` command and its query result
    for a keyboard/a11y printer chooser; do not add a second print command.
  - If explicit non-origin print-title bands require additive `PageSetup`
    fields, document the frozen-contract addition and migration in the RFC
    section before merge. Existing serialized workbooks must retain defaults.
  - No IPC, MCP, workbook-card, or existing command schema changes are planned.
- Tests and corpora to write first:
  - Repository/package lints for PKGBUILDs, installed-path manifests, desktop
    MIME coverage, icons, rename identity, release workflow, and generated-doc
    drift; an Arch container smoke script used by CI.
  - AccessKit tree tests for grid/panel/palette, a keyboard-only workflow, split
    panes and clickable status tests, reduced-motion and font-cache tests.
  - XLSX/OMC/PDF print-title corpora covering non-origin row/column title bands,
    first-page/repeat behavior, undo, and round-trip.
  - Performance-baseline schema/validator tests and nightly workflow checks for
    every parser fuzz target.
- Items the package says to decide and document and the decision taken:
  - Keep the existing one-workbook-per-process/window model for 1.0; the final
    one-process-per-file ADR remains a HUMAN / WP-28 G4 live-workflow decision,
    so this package will not invent in-process multi-window sharing.
  - Use the existing `file.print` schema and a retained accessible panel for
    printer selection, reachable from the keyboard/palette.
  - Package `ttf-carlito` and `ttf-liberation`, resolve Calibri/Arial through
    their metric-compatible aliases, and embed the resolved face in PDF output.
    Standard-14 Helvetica remains the documented last-resort contract only
    when fontconfig finds no usable face; Aptos metrics remain a known
    difference because no free metric clone exists.
  - Fluent resources are shipped as the localization scaffold. The extraction
    lint will make the owned release UI inventory explicit without pretending
    that workbook data, formulas, command ids, or diagnostics are translatable.
  - Software-render CI is a smoke/regression lane only. Product cold-start and
    presentation-aware scroll budgets run on the labeled fixed integrated-GPU
    host with committed 10% thresholds.
- Open questions at planning time:
  - **HUMAN / G7:** Omarchy approval/trademark clearance for the Omacell name is
    required before a public tag, but does not block implementation or
    non-release CI.
  - **HUMAN / WP-28 G4:** live stable/RC/edge Omarchy VM runs, fixed integrated-
    GPU measurements, CJK IME, Orca, and the physical terminal matrix require
    the corresponding hosts and remain visible release gates.
  - Final AUR binary asset checksums and release notes are generated from the
    signed/tagged release; pre-tag CI validates their templates and artifact
    layout rather than fabricating release hashes.

## What was built

WP-28 now has a complete distribution surface instead of release placeholders:

- `packaging/` contains source and binary PKGBUILDs, a post-install hook,
  desktop/MIME registrations, the complete hicolor icon set, and an unprivileged
  clean-Arch smoke harness. LibreOffice is not a runtime or package dependency;
  native read-only `.xls` support remains available.
- `docs/book/`, `book.toml`, the CLI example generator, and the generation/drift
  scripts provide a buildable user manual whose CLI, configuration, and Lua
  references are derived from the shipped APIs. Parser limits, AI/privacy,
  Omarchy, and printing/font behavior are release documentation rather than
  tribal knowledge.
- Packaging, Omarchy stable/RC/edge, fixed-host performance, nightly fuzz and
  dependency checks, and tag-release workflows are defined under
  `.github/workflows/`. The release job is held behind the external public-name
  environment gate.
- The GUI release pass adds explicit Fluent-backed user-facing strings,
  reduced-motion behavior, four-quadrant split-pane painting, per-family font
  caching, clickable status actions, and retained keyboard/AccessKit panels for
  printer selection. Freeze and split traversal is bounded even when hostile
  workbook geometry hides or spans most of an axis. Tests cover the grid,
  palette, panels, status actions, split panes, font cache, reduced motion, and
  a keyboard-only workflow.
- Printing now represents arbitrary repeated row and column title bands,
  round-trips Excel `_xlnm.Print_Titles`, repeats those bands in PDF pagination,
  selects a metric-compatible installed face for PDF embedding, and exposes a
  persistent last-printer chooser through the existing `file.print` command.
- `benchmarks/release-budgets.json`, its validator, `just perf-check`,
  `just docs-check`, `just i18n-check`, repository release lints, the changelog,
  and `scripts/rename.sh` make the release assumptions mechanically visible.

## Interfaces exposed (for dependents)

- `PrintTitleBand<T> { start, end }` and `PageSetup::{title_row_band,
  title_col_band}` are the canonical non-origin print-title representation.
  `PageSetup::{row_title_band,col_title_band}` also migrate legacy origin-count
  fields at read time.
- `file.print {path?, printer?}` keeps its frozen argument schema. Its query
  result additively reports `printers`, `last_printer`, `printed`,
  `font_embedded`, and `warning`; choosing a printer persists only its name in
  the application state directory.
- `/usr/share/omacell/{default,i18n}`, the desktop/MIME/icon paths, shell
  completions, `omacell(1)`, and `/usr/share/doc/omacell/manual` are the stable
  installed distribution layout. `PKGBUILD-bin` consumes architecture-specific
  release-tree `.tar.zst` bundles and wraps them as an Arch package.
- `scripts/check-perf-baselines.py [results.jsonl]` validates all 17 §12.1
  metrics, the exact 10 percent boundary, result completeness, and direction.
  Fixed hosts provide the JSON-lines path through `OMACELL_PERF_RESULTS`.
- `scripts/extract-i18n.py --check`, `scripts/generate-docs.py --check`, and the
  Clap reference example are reusable drift gates for future UI/API changes.

## Deviations from the spec or the package (with reasons)

- The app retains the already-shipped one-workbook-per-process/window model.
  Sharing multiple native windows in one process is not needed for 1.0 and
  cannot be selected responsibly without the G4 live workflow/Wayland pass.
- Software kittest render/readback remains a regression smoke, not a claim
  about surface presentation latency. The workflow rejects non-integrated
  adapters and separately requires the fixed host's complete product-measurement
  export before considering the 17 release budgets green.
- No fallback TTF is copied into the repository. Arch packages depend on
  redistributable Carlito and Liberation families; PDF export embeds the
  resolved face. Standard-14 Helvetica is retained only as a warning-bearing
  final fallback when fontconfig supplies no face.
- Full chart property editing stays in WP-28a. WP-28 closes the release work
  assigned here without silently absorbing that separately owned feature.

## Measurements

- `CARGO_TARGET_DIR=/home/duncan/.cache/omacell/target just check` passed on
  2026-09-01: formatting, strict all-target Clippy, all workspace unit and
  integration tests (including LibreOffice/openpyxl cross-checks), doctests,
  and warning-free public documentation.
- `/home/duncan/.cargo/bin/mdbook build`, `just docs-check`, and
  `scripts/extract-i18n.py --check` passed with no generated-reference or
  localization drift.
- `scripts/check-perf-baselines.py` accepted all 17 metrics at their exact 10
  percent boundaries. Product measurements remain a fixed-host G4 result, not
  a fabricated local number.
- `cargo deny check` passed advisories, bans, licenses, and sources. Independent
  `cargo audit` found no vulnerabilities and reported only the two explicitly
  allowed unmaintained transitive crates (`paste` and `ttf-parser`).
- `desktop-file-validate packaging/omacell.desktop`, `xmllint --noout
  packaging/omacell.xml`, workflow YAML parsing, `git diff --check`, XLSX print
  corpus tests, PDF pagination tests, and GUI AccessKit/keyboard tests passed.
- The Docker clean-Arch command could not run on this workstation because its
  daemon socket is inaccessible and sudo requires an interactive password.
  `.github/workflows/packaging.yml` runs that exact required build/install smoke
  on every packaging PR; its status is therefore CI evidence, not claimed here.

## Open questions / decisions needed

- **HUMAN / G7:** record public-name/trademark clearance in the
  `public-release` environment (`OMACELL_NAME_CLEARED=true`) before a tag can
  publish.
- **HUMAN / WP-28 G4:** obtain green stable, RC, and edge Omarchy runners;
  collect the full fixed integrated-GPU result JSONL; and complete the CJK IME,
  Orca, and physical terminal walkthroughs.
- **HUMAN / WP-28 G4:** confirm the one-workbook-per-process/window decision as
  the final 1.0 ADR after the live Wayland workflow test.
- The clean Arch and live channel workflows must be green on the PR. Local
  static validation is not substituted for their privileged runner evidence.

## RFC (only if a frozen contract changed)

`PageSetup` is a frozen serialized core type. This package additively introduces
`title_row_band: Option<PrintTitleBand<u32>>` and
`title_col_band: Option<PrintTitleBand<u16>>` because Excel print titles may
start away from the print-area origin. Existing `title_rows`/`title_cols` counts
remain readable and are converted by the accessors, so old `.omc` data keeps
its behavior. New XLSX/PDF paths use explicit inclusive bands. Omitting the new
fields preserves the old serialized default, and no existing field changes
meaning.

The frozen `file.print` arguments are unchanged. The result gains only optional
status fields so older callers can ignore them. A second print command was
rejected because it would split one operation across two public schemas;
retaining only origin counts was rejected because it cannot represent Excel's
`$3:$4` or `$B:$C` titles. Merge of this PR records the required approval for
both additive contract changes.

## Checklist

- [x] `just check` green with the required persistent Cargo target directory.
- [x] Every agent-verifiable acceptance item has evidence; privileged/live human
  gates are named explicitly and are enforced by required workflows.
- [x] Generated references have zero drift; mdBook and Rust API docs build.
- [x] The complete §12.1 budget table and 10 percent boundaries are committed.
- [x] No new `TODO(` lacks a `WP-` reference; dependency policy and audit pass.
- [x] Validation side effects outside the repository are limited to documented
  Cargo caches/tools; no package or Omarchy path on the host was modified.

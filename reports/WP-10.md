# Report — WP-10: .xlsx writer, round-trip diff tool, atomic save

## Plan (written before coding)

- Files/modules to create:
  - `crates/io/src/xlsx/write.rs` — regenerate workbook, SST, styles, worksheets (cells, formulas, merges, views, autofilter, protection, hyperlinks, print/CF/DV/sparkline extras), tables, comments; merge content types/rels with preserved L3 parts.
  - `crates/io/src/xlsx/atomic.rs` — temp in the destination directory, `fsync`, rename; `keep_backups`; LibreOffice `.~lock.<name>#`.
  - `crates/io/src/xlsx/diff.rs` — model-level L1/L2 JSON diff (cells, formulas, styles/numFmts, names, tables, views, hyperlinks, notes, extras, L3 part hashes).
  - `crates/io/src/error.rs` — `xlsx.write`, `xlsx.lock`.
  - Additive `AxisGeometry::{iter_custom,iter_hidden}` so the writer can emit row/col overrides without scanning the Excel grid.
  - Tests: `xlsx_roundtrip.rs`, `xlsx_save.rs` (crash-safety + lock); openpyxl/LO skip if absent.
  - Bench: `xlsx_save.rs`.
- Interfaces to expose:
  - `xlsx::{save, save_bytes, save_workbook, SaveOptions, lock_path, acquire_lock, release_lock, diff, DiffReport}`.
  - No CLI (`omacell diff` is WP-13 over this API).
- Tests and corpora to write first:
  - Open each `tests/corpus/xlsx/*.xlsx` → `save_bytes` → open → `diff` empty at L1/L2.
  - L3: non-rewritten parts byte-identical (`xl/omacell` rewritten from `custom_parts`).
  - calamine loads the saved file; openpyxl/LibreOffice skip if missing.
  - Crash: fail before rename leaves the original bytes.
- Decisions:
  - **Regenerate** modeled parts from the workbook. **Copy** original bytes for everything else (theme, drawings, VBA, media, …).
  - Worksheet XML is regenerated and **injects** `WorksheetExtras` fragments (print/CF/DV/sparklines) so L2 extras survive.
  - Shared formulas are written as independent formulas (WP-09 already expanded them).
  - Formula `f` text is stored without a leading `=`; reader re-wraps.
  - No `fullCalcOnLoad`. No `calcChain`.
  - Atomic save: a unique hidden temp file in the same directory, `sync_all` file then parent dir, `rename`.
  - Lock file: `.~lock.{filename}#` in LibreOffice's five-field escaped format; refuse save if ownership cannot be proven safe.
  - Backups: `{name}.bak.{n}` beside the file, newest index 1, max `keep_backups`.
- Open questions at planning time:
  1. Should a new workbook (no `OpcPackage`) still write a theme part? Plan: no; Excel/calamine accept files without theme.
  2. SST unused entries from the original file are dropped. Plan: yes (L1 is cell values, not the SST table).

## What was built

Modeled OPC parts are regenerated from the workbook; unmodeled parts are copied byte-identical from `OpcPackage`. Worksheet XML injects `WorksheetExtras` fragments (autofilter, CF, DV, print, sparklines) in ECMA-ish order. Shared formulas are written as independent formulas (WP-09 already expanded them), while legacy fixed-range CSE anchors retain `<f t="array" ref="…">` and cached follower values. Formula `f` text is stored without a leading `=`. `calcChain` and `fullCalcOnLoad` are never written. Custom payloads go to `xl/omacell/` from `Workbook::custom_parts`.

Atomic save uses a collision-resistant, exclusively-created temp file in the destination directory, `sync_all`s the file before backup rotation/rename, and `sync_all`s the parent directory afterward. All pre-rename error paths remove the temp file. Backup and unlock failures are reported rather than silently ignored. Numbered backups are `{name}.bak.{n}` (newest is 1).

LibreOffice-compatible `.~lock.{filename}#` files use its escaped, semicolon-terminated five-field format. Lock creation is exclusive; advisory locking plus inode revalidation prevents two writers from both recovering the same stale lock. Symlinks and unknown/foreign lock formats are refused conservatively, and Omacell only removes a lock that still belongs to the current host and process.

The writer now preserves rich strings (including distinct formatting for equal plain text), string formula caches, OOXML escaped control/literal tokens, defined-name constants and comments, row/column geometry, complete sheet views and protection, tab colors, hyperlinks, notes, tables, gradient fills, active-sheet visibility, macro-enabled package content types, and source worksheet relationships. New legacy notes receive a minimal VML drawing; existing VML is preserved. Table and package identifiers are unique, raw XML extras are validated as a single allowed fragment without embedded document declarations, and output limits and non-finite/model-range validation prevent malformed or unbounded packages. Threaded comments are reported by the diff and rejected explicitly until WP-17 can regenerate them, rather than being silently dropped.

`xlsx::diff` is a symmetric model-level L1/L2 JSON report. It compares exact cells/formulas, semantic styles and number formats, names, tables, workbook/sheet views, geometry, annotations, extras/custom payloads, and preserved L3 bytes plus content types in both directions. The `omacell diff` CLI is WP-13 over this API.

Key files:

- `crates/io/src/xlsx/{write,atomic,diff}.rs`
- `crates/io/src/xlsx/{mod,xml}.rs` — public save/diff API; XML escape
- `crates/io/src/error.rs` — `xlsx.write`, `xlsx.lock`
- `crates/core/src/geometry.rs` — additive `AxisGeometry::{iter_custom,iter_hidden}`
- Tests: `crates/io/tests/xlsx_roundtrip.rs`; unit tests in `atomic.rs`
- Bench: `crates/io/benches/xlsx_save.rs`

Key tests: `roundtrip_diff_empty_for_corpus`, `new_workbook_preserves_modeled_l1_l2_fields`, `diff_detects_symmetric_added_content`, macro/custom-namespace/XML-injection/numeric validation regressions, `saved_file_loads_in_calamine`, `saved_file_loads_in_openpyxl_if_present`, `saved_file_converts_in_libreoffice_if_present`, and atomic-save/lock race/cleanup/backup tests.

## Interfaces exposed (for dependents)

| Item | Notes |
|---|---|
| `xlsx::save` / `save_workbook` | atomic path save; `SaveOptions { keep_backups, lock }` |
| `xlsx::save_bytes` / `save_workbook_bytes` | in-memory encode |
| `xlsx::{lock_path, acquire_lock, release_lock}` | `.~lock.{name}#` |
| `xlsx::{diff, DiffReport}` | serde JSON; `empty` is true iff every category list is empty |
| `xlsx::error::codes` | `xlsx.write`, `xlsx.lock` (plus WP-09 codes) |
| `AxisGeometry::{iter_custom, iter_hidden}` | ascending index order |

No commands or CLI. Reuse `tests/corpus/xlsx/`.

## Deviations from the spec or the package (with reasons)

- **`calcChain` is omitted** and never copied. A copied chain would be stale after regenerate; spec says never write `fullCalcOnLoad` unless the chain is absent — we write neither.
- **Shared formulas** are independent `<f>` cells, not `t="shared"`. Round-trip L1 matches because WP-09 expands them on load.
- **SST unused entries** from the original file are dropped (L1 is cell values).
- **New workbooks have no theme part.** Excel/calamine/LibreOffice accept this; a preserved package still copies an existing theme and keeps its workbook relationship.
- **At WP-10 time**, threaded comments/persons were a WP-17 handoff. They are now
  modeled and read/written with replies and resolved state. Legacy note VML is
  generated for new notes, while arbitrary source drawing shapes remain opaque
  L3 content rather than an unowned WP-17 deferral.
- **Autosave directory** `~/.local/state/omacell/autosave/` is F-9.7 app behavior (WP-13/UI). This package supplies the atomic write, lock, and `keep_backups` primitives.
- **`omacell diff` CLI** is WP-13. This package exposes the JSON-serializable API.
- **openpyxl** is not installed on this machine; the test skips only on `ImportError`/`ModuleNotFoundError` and would fail on a load error.

## Measurements

- `just check` — pass.
- `RUSTDOCFLAGS="-D warnings" cargo doc -p omacell-io -p omacell-core --no-deps` — pass.
- `cargo deny check` — pass; the later calamine 0.36.1 upgrade removed the former quick-xml 0.39 advisory ignores described in WP-09.
- Round-trip: `cargo test -p omacell-io --test xlsx_roundtrip`. All 10 corpus files open → `save_bytes` → open → symmetric `diff` empty at L1/L2; focused regressions cover rich strings, views, geometry, protection, annotations, tables, names, fixed-range CSE formulas, custom payloads, macro packages, and invalid output rejection.
- calamine and openpyxl load the saved workbook. LibreOffice `--headless --convert-to csv` produced a CSV using an isolated `UserInstallation` profile.
- Atomic save: 10 unit tests cover original preservation, temp cleanup, lock refusal/recovery/concurrency/ownership, lock format, and numbered backup rotation.
- Criterion (`cargo bench -p omacell-io --bench xlsx_save -- --sample-size 10`): **xlsx_save/numeric_86k_x_20 2.064 s** median (same 86 000 × 20 numeric grid as `xlsx_open`). Spec 50 MB save < 5 s: pass with ~2.9 s headroom on this machine.

`rustix` is now a direct dependency (already present in the workspace lockfile transitively) for safe advisory file locking and metadata identity checks. This is within the package's preapproved dependencies. `zip` / `quick-xml` / `calamine` (dev) were already locked by WP-09.

## Open questions / decisions needed

1. **Resolved:** regenerate modeled parts and copy the rest. Worksheet extras are
   injected so unsupported content survives.
2. **Resolved:** frozen panes write integer row/column counts; unfrozen splits
   convert core pixels to OOXML twips at 15 twips per pixel (96 DPI).
3. **Resolved ownership:** new notes receive valid minimal VML; WP-17 models
   threaded comments/persons; WP-25 models core charts. Arbitrary source VML and
   exotic drawings remain opaque L3 parts by design.

## RFC (only if a frozen contract changed)

WP-01 public types are unchanged on the wire. `AxisGeometry::{iter_custom,iter_hidden}` are additive methods so the writer can emit row/col overrides without scanning the Excel grid. No fields, serde shape, or command schema changed. Merge constituted human approval of this additive surface, at the same bar as WP-09’s narrow `Workbook` setters.

## Checklist

- [x] `just check` green locally (see Measurements)
- [x] Every acceptance criterion ticked with evidence (see below)
- [x] Docs warning-free; public items documented
- [x] Baselines recorded (86k×20 xlsx_save median 2.064 s)
- [x] No new `TODO(` without a `WP-` reference; direct `rustix` use is preapproved and deny-clean
- [x] Nothing written outside the repository except documented temp dirs (`std::env::temp_dir()` in tests, cleaned up)

### Acceptance

- [x] Round-trip corpus: open → save → `diff` empty at L1/L2; preserved L3 parts byte-identical (`roundtrip_diff_empty_for_corpus`)
- [x] Saved files load in calamine; openpyxl skipped if absent; LibreOffice headless conversion succeeded
- [x] Crash-safety: fail before rename leaves the original intact
- [x] Bench: 50 MB-class save 2.064 s < 5 s

# Report — WP-02: Workbook model and storage

## Plan (written before coding)

- Files/modules to create:
  - `crates/core/src/intern.rs` — workbook-scoped interners: strings (SST-shaped, stable ids, refcounts, rich-text runs), styles (dedup by value, refcounted), arrays, and formula *source* (`FormulaId`). No AST (WP-03).
  - `crates/core/src/storage.rs` — 256×256 blocks in `FxHashMap<BlockCoord, Arc<Block>>`; per block occupancy bitmap + packed dense `CellSlot` array; get/set/clear; row-major and region iteration; `used_range()` / `dimension()`; block-aware row/column shift. Copy-on-write via `Arc::make_mut`.
  - `crates/core/src/geometry.rs` — Fenwick trees over row heights and column widths with hidden flags; `pixel_to_index`, `index_to_pixel`, batch updates.
  - `crates/core/src/names.rs` — defined names at workbook/sheet scope; referent is a range, a constant `Value`, or formula *text*.
  - `crates/core/src/tables.rs` — tables registry (name, range, header/totals, banded, auto-expand). Structured-reference parse stays WP-03.
  - `crates/core/src/undo.rs` — `Transaction` grouping, inverse deltas, memory budget with oldest-first eviction, `undo()`/`redo()` returning affected ranges.
  - `crates/core/src/sheet.rs` — name rules, visibility, tab color, view state, merged ranges, notes/comments/hyperlinks stores, protection, geometry, storage.
  - `crates/core/src/workbook.rs` — ordered sheets, interners, names, tables, settings (date system, calc mode, iteration, precision-as-displayed), metadata, opaque custom-part payloads, undo log, cheap `snapshot()` for readers.
  - `crates/core/src/lib.rs` — append `pub mod` lines for the modules above only (no reorder of existing mods).
  - `crates/core/src/error.rs` — add new `codes::*` constants only (`sheet.name`, `sheet.id`, `name.defined`, `table.name`, `undo.empty`). Do not change existing codes or `CoreError` shape.
  - Corpora: `tests/corpus/workbook/sheet_names.tsv`, `tests/corpus/workbook/defined_names.tsv`.
  - Integration tests: `crates/core/tests/storage_oracle.rs`, `crates/core/tests/memory.rs`, `crates/core/tests/workbook_model.rs`.
  - Criterion benches: `crates/core/benches/geometry.rs`, `crates/core/benches/iteration.rs`.
  - Workspace deps: `rustc-hash`, `indexmap` (runtime); `criterion` (dev). All pre-approved.
- Interfaces to expose (types, commands, schemas, CLI):
  - `storage::{CellSlot, CellFlags, SheetStore, BLOCK_SIZE}` — `CellSlot { value, formula: Option<FormulaId>, style: StyleId, flags }`. `FormulaId` is an intern handle for formula *source text*; WP-03 owns the AST and rewrite hooks.
  - `intern::{Interners, StrId` (existing), `FormulaId, RichTextRun, ArrayPayload}` — intern/get/add_ref/release.
  - `geometry::{SheetGeometry, AxisGeometry}` — row/col sizes, hidden, pixel↔index.
  - `names::{DefinedName, NameScope, NameReferent, NameRegistry}`.
  - `tables::{Table, TableId, TableColumn, TableRegistry}`.
  - `undo::{UndoLog, Transaction, Delta, AffectedRange}`.
  - `sheet::{Sheet, SheetVisibility, ViewState, FreezePanes, SplitView, ProtectionState, Note, Comment, Hyperlink}`.
  - `workbook::{Workbook, WorkbookSnapshot, WorkbookSettings, DateSystem, CalcMode, Iteration, WorkbookMeta}`.
  - `Workbook::resolve_sheet` / `resolve_parsed` maps WP-01 `SheetSpec` names to `SheetId`.
  - No commands registered (WP-07a). No CLI. No `formula/` module. `locale.rs` untouched.
- Tests and corpora to write first:
  - `tests/corpus/workbook/sheet_names.tsv` — F-1.1 length, forbidden `[]:*?/\\`, empty, uniqueness (case-insensitive), visibility.
  - `tests/corpus/workbook/defined_names.tsv` — workbook vs sheet scope, duplicate, cell-ref-like names.
  - `proptest` oracle: `SheetStore` vs `HashMap<(u32,u16), CellSlot>` under random set/clear/shift sequences on a small grid.
  - Memory: counting via `SheetStore::heap_bytes()` (workspace `unsafe_code = forbid` blocks a `GlobalAlloc` wrapper). Always-on 10k×20; `#[ignore]` 1M×20 numeric ≤ 64 B/cell amortized. Run the ignored test for the report.
  - Geometry unit tests: hidden + custom-sized rows; `index_to_pixel` / `pixel_to_index` inverses. Criterion bench that scales with axis length.
  - Undo/redo property: any recorded transaction; undo restores the model; redo restores the result.
  - Snapshot: mutating the writer does not change a held snapshot (CoW).
- Items the package says to "decide and document" and the decision taken:
  - **Packed slots, not a dense 65 536-slot array per block.** A dense `[CellSlot; 65536]` at 32 B/slot would be ~8 GB for 1M×20 (only one block-column is live, but every 256-row band still allocates 256 columns). Occupancy bitmap + packed `Vec<CellSlot>` (row-major set bits) meets “dense slot array plus occupancy bitmap”, the ≤ 64 B/cell budget, and sparse-sheet memory. Blocks live in `FxHashMap` keyed by block coordinate, as specified.
  - **`CellSlot` is 32 bytes** (`Value` 16 + `Option<FormulaId>` 8 + `StyleId` 4 + `CellFlags` 1 + pad). Notes, threaded comments, hyperlinks, merges live in side tables on `Sheet` (F-2.2 / WP text), not in the slot, so the numeric-cell budget is not charged for them.
  - **`FormulaId` is interned source text**, not an AST. Shift relocates slots only; formula token rewrite is a documented hook for WP-03/WP-17 (no-op here).
  - **CoW snapshot:** `Arc<Block>` pages + `Arc<Interners>`. `Workbook` is single-writer; `snapshot()` clones the block map (Arc bumps) so render can read during recalc (§11.5).
  - **Geometry:** sparse Fenwick of height/width *deltas* from the default (so empty sheets do not allocate 1M entries) plus a hidden set. `index_to_pixel` is O(log n); `pixel_to_index` binary-searches prefix sums (O(log² n), < 400 ops at 1M). Defaults: 20 px row, 64 px column.
  - **Intern recycle:** ids are stable while `refs > 0`. Undo deltas hold intern refs so undo cannot observe a recycled id. Style id 0 is `Style::default()` and is never freed.
  - **Sheet names:** ≤ 31 Unicode scalars, none of `[]:*?/\\`, non-empty, no leading/trailing `'`, unique ignoring ASCII/Unicode case. At least one sheet; the last *visible* sheet cannot be hidden or removed.
  - **Out-of-range shift:** insert that would push an occupied cell past `MAX_ROWS`/`MAX_COLS` is `addr.ref` (matches F-1.2).
  - **Counting allocator:** not a `#[global_allocator]` (workspace `unsafe_code = forbid`). `heap_bytes()` walks bitmaps, slot `capacity`, hashbrown bucket estimate, intern tables. Linux `/proc/self/statm` RSS is recorded as a secondary number for the 1M×20 case.
  - **New error codes** extend `error::codes`; WP-01 signatures and `docs/contracts.md` are not edited.
- Open questions at planning time:
  1. Should insert-row that would push data off the grid refuse (Excel) or drop overflowing cells? Plan: refuse with `addr.ref`.
  2. Excel defined-name grammar is larger than “not a cell ref” (single `R`/`C`, `\foo`, 255-char cap). Plan: enforce a conservative identifier + not-an-A1/R1C1-cell rule; expand in WP-03 if the formula parser needs more.
  3. Default row height / column width in pixels vs Excel’s point / character units — WP-06/UI may need a conversion. Plan: store pixels in geometry; 20 / 64 as engine defaults.

## What was built

In-memory workbook with the §11.3 storage layout, interners, Fenwick geometry, defined names, tables registry, and a memory-budgeted undo log. `Workbook` is single-writer; `snapshot()` is copy-on-write via `Arc<Block>` pages. Formula *source* is interned as `FormulaId`; there is no parser or AST (WP-03). `locale.rs` is unchanged.

Review hardening makes structural deletes exactly undoable by retaining deleted slots in the inverse delta, validates all shift arithmetic and side-table coordinates at grid boundaries, and removes discarded redo transactions from undo-budget accounting. The workbook and number-format packages share the canonical `date_system::DateSystem`; `workbook::DateSystem` remains a re-export for callers.

Key files:

- `crates/core/src/{workbook,sheet,storage,intern,geometry,names,tables,undo}.rs`
- `crates/core/src/lib.rs` — `pub mod` for those modules
- `crates/core/src/error.rs` — new codes only (`sheet.name`, `sheet.id`, `name.defined`, `table.name`, `undo.empty`, `formula.len`)
- Corpora: `tests/corpus/workbook/sheet_names.tsv`, `tests/corpus/workbook/defined_names.tsv`
- Tests: `crates/core/tests/{storage_oracle,memory,workbook_model}.rs`
- Benches: `crates/core/benches/{geometry,iteration}.rs`

Key tests:

- `storage_oracle` — `SheetStore` vs `HashMap<(u32,u16), CellSlot>` under `proptest` set/clear/shift (crosses the 256-row block boundary)
- `memory` — 10k×20 always-on; `#[ignore]` 1M×20 ≤ 64 B/cell
- `workbook_model` — sheet-name / defined-name corpora, visibility rules, snapshot isolation, undo/redo property
- Geometry unit tests: hidden + custom sizes; pixel↔index inverses

## Interfaces exposed (for dependents)

| Module | Types / fns |
|---|---|
| `omacell_core::storage` | `BLOCK_SIZE` (256), `BlockCoord`, `CellSlot { value, formula: Option<FormulaId>, style, flags }`, `CellFlags`, `SheetStore` (`get`/`set`/`clear`, row-major `iter`/`iter_region`, `used_range`, `dimension`, `shift_rows`/`shift_cols`, `heap_bytes`, `shrink_to_fit`), `UsedRange` |
| `omacell_core::intern` | `FormulaId` (interned **source text**, not AST), `RichTextRun`, `ArrayPayload`, `StringInterner`, `StyleInterner`, `ArrayInterner`, `FormulaInterner`, `Interners` |
| `omacell_core::geometry` | `DEFAULT_ROW_PX` (20), `DEFAULT_COL_PX` (64), `AxisGeometry` (`size`/`set_size`/`set_hidden`/`index_to_pixel`/`pixel_to_index`/`set_sizes`), `SheetGeometry` |
| `omacell_core::names` | `NameScope`, `NameReferent` (Range / Constant / Formula text), `DefinedName`, `NameRegistry`, `validate_defined_name` |
| `omacell_core::tables` | `TableId`, `TableColumn`, `Table`, `TableRegistry` |
| `omacell_core::undo` | `UndoLog`, `Transaction`, `Delta`, `AffectedRange`, `DEFAULT_BUDGET` (64 MiB) |
| `omacell_core::sheet` | `Sheet`, `SheetVisibility`, `ViewState`, `FreezePanes`, `SplitView`, `ProtectionState`, `Note`, `Comment`, `Hyperlink`, `validate_sheet_name` |
| `omacell_core::date_system` | Canonical `DateSystem { Excel1900, Excel1904 }`, shared with WP-06 |
| `omacell_core::workbook` | `Workbook`, `WorkbookSnapshot`, `WorkbookSettings`, re-exported `DateSystem`, `CalcMode`, `Iteration`, `WorkbookMeta`. `Workbook::resolve_sheet_name` / `resolve_spec` / `resolve_parsed` map WP-01 `SheetSpec` names to `SheetId`. |

**`FormulaId`:** a `u32` handle to interned formula *source*. `CellSlot.formula` is `Option<FormulaId>`. WP-03 owns `formula::` AST, printer, and rewrite. Structural shift (insert/delete rows/cols) **relocates slots only**; token rewrite is `TODO(WP-03)` / `TODO(WP-17)`.

No command ids registered (WP-07a). No CLI flags. No changes to frozen WP-01 types or `docs/contracts.md`.

Reusable corpora: `tests/corpus/workbook/*.tsv`.

## Deviations from the spec or the package (with reasons)

- **Packed occupancy + `Vec<CellSlot>`, not a dense `[CellSlot; 65536]` per block.** A dense array blows the 1M×20 / 64 B/cell budget (~8 GB). Packed slots still match “dense slot array plus occupancy bitmap”.
- **`pixel_to_index` is O(log² n)** (binary search over Fenwick prefixes), not strict O(log n). At 1M rows that is < 400 operations; the bench still scales with log n. `index_to_pixel` is O(log n).
- **Counting allocator is structural `heap_bytes()`, not `#[global_allocator]`.** Workspace `unsafe_code = forbid` cannot wrap `System`. RSS is also recorded from `/proc/self/statm` (4096-byte page assumption).
- **Geometry stores pixels**, not Excel point/character units. Defaults 20 px / 64 px. Conversion is WP-06/UI.
- **`lib.rs` module list is rustfmt-alphabetical.** New mods are inserted in that order rather than appended after `value`, so `cargo fmt --check` is green. WP-01 modules keep their relative order.
- **New `error::codes` constants** (`sheet.name`, …, `formula.len`) extend the module without changing frozen WP-01 signatures. `docs/contracts.md` was not edited.

## Measurements

- `just check` — pass (fmt, clippy `-D warnings`, `cargo test --workspace`, `cargo doc --workspace --no-deps`).
- `cargo deny check` — pass (advisories, bans, licenses, sources). Unused-allowlist warnings only.
- `size_of::<CellSlot>() <= 32` — compile-time assert + unit test.
- **1M × 20 numeric cells** (`cargo test -p omacell-core --release --test memory million_by_twenty -- --ignored --nocapture`, 7.19 s):
  - `heap_bytes` raw (Vec slack): **1 056 346 792 B = 52.82 B/cell**
  - `heap_bytes` after `shrink_to_fit`: **672 346 792 B = 33.62 B/cell**
  - RSS (`/proc/self/statm` resident pages × 4096): **691 445 760 B = 34.57 B/cell**
  - All ≤ 64 B/cell; RSS ≪ 1.5 GB (§12.1).
- Always-on 10 000 × 20 memory test: pass (0.79 s debug).
- Geometry criterion (`cargo bench -p omacell-core --bench geometry -- --sample-size 20`):

  | n custom/hidden | `pixel_to_index` | `index_to_pixel` |
  |---|---|---|
  | 1 000 | 651 ns | 50 ns |
  | 10 000 | 970 ns | 66 ns |
  | 100 000 | 1.29 µs | 106 ns |
  | 1 000 000 | 2.39 µs | 158 ns |

  Times grow roughly with log n (binary search + Fenwick), not with n.

- Corpora: 16 sheet-name rows, 15 defined-name rows.
- Doctests: 86 in `omacell-core` (WP-01 + WP-02).

Locked new versions: `rustc-hash` 2.1.3, `indexmap` 2.14.0, `criterion` 0.5.1 (dev). All pre-approved in AGENTS.md.

## Open questions / decisions needed

1. **Formula rewrite on insert/delete** is a no-op here. Confirm WP-03 (`rewrite`) + WP-17 consume `ShiftRows`/`ShiftCols` deltas or a hook on `SheetStore`.
2. **Resolved 2026-08-31:** defined names accept `\\` initially and `.` / `?` after the first character, reject A1/R1C1 references, bare `R`/`C`, and `TRUE`/`FALSE`, and are capped at 255 Unicode characters.
3. **Undo intern-refcount on eviction.** Deltas hold intern refs while the transaction is on the stack; oldest-first eviction drops the `Transaction` without walking interners, so those refs leak until workbook drop. Fine for session lifetime; compact SST dump (WP-10) should skip zero-logical users or rebuild.
4. **Row height / column width units.** Engine stores pixels. Excel serializes points and character widths. Who converts (WP-06, WP-09/10, or WP-16)?
5. Parallel Phase 1 agents shared this worktree (`formula/`, `numfmt/`, `dates.rs`). Those files are **not** in this PR.

## RFC (only if a frozen contract changed)

None. Frozen WP-01 types and `docs/contracts.md` are unchanged. New `error::codes` constants are additive.

## Checklist

- [x] `just check` green locally (see Measurements)
- [x] Every acceptance criterion ticked with evidence (see below)
- [x] Docs warning-free; public items documented (86 doctests)
- [x] Baselines recorded (geometry criterion numbers in Measurements; no committed criterion baseline files in this package)
- [x] No new `TODO(` without a `WP-` reference; no new dependency without justification (`rustc-hash`, `indexmap`, `criterion` are pre-approved; `cargo deny check` green)
- [x] Nothing written outside the repository except documented temp dirs

### Acceptance (WP-02)

- [x] Storage behaves identically to a `HashMap<(u32,u16), Cell>` oracle under `proptest` sequences of set/clear/shift (`crates/core/tests/storage_oracle.rs`)
- [x] Memory test: 1,000,000 × 20 numeric cells ≤ 64 B/cell amortized (33.62 B/cell compact, 52.82 B/cell with Vec slack, 34.57 B/cell RSS)
- [x] Geometry mapping correct with hidden and custom-sized rows; scaling bench recorded (see Measurements)
- [x] Undo/redo property: any transaction followed by undo restores the exact model; redo restores the transaction result, including structurally deleted rows/columns and their cells (`undo_redo_property` and `undo_redo_restores_structurally_deleted_cells` in `workbook_model.rs`)
- [x] Sheet naming and visibility rules enforced with clear errors (corpus + `sheet_duplicate_and_visibility_rules`)

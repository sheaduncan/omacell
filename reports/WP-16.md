# Report — WP-16: GUI foundation (eframe/egui on wgpu)

## Plan (written before coding)

### File-lifecycle completion (2026-09-01)

- Register the deferred `file.new`, `file.close`, and `file.saveas` composition
  commands alongside the existing file adapters, with typed schemas and catalog
  coverage.
- Reuse `WorkbookOpened { path: None }` for the new-workbook transaction so the
  bus clears workbook-scoped changesets/repeat state, while the file sidecar
  drops the prior path/package and retains process-scoped config/AI services.
- Reconcile successful new/save-as/close outcomes in both GUI and TUI. Guard
  destructive new/open/close actions with the existing unsaved-change confirmation
  pattern, and route key-bound commands with required arguments into the shared
  schema prompt.
- Write command-adapter and frontend lifecycle regressions first, then update
  the command contract/report and run the full repository gate.

- Files/modules to create:
  - `crates/gui/src/{lib,app,theme,grid,chrome,input}.rs` — eframe app over WP-14 + WP-15a
  - `crates/gui/tests/{common,snapshots,reload,a11y}.rs`
  - `crates/gui/benches/scroll.rs` — 1M-row software-raster frame budget
  - CLI: bare `omacell [file]` launches GUI; no display → exit 1 (`gui.display`)
- Interfaces:
  - `omacell_gui::{Launch, Gui, run}`
  - `omacell [file]`
- Tests first:
  - `egui_kittest` snapshots at 1× / 1.5× / 2× × tokyo-night, catppuccin-latte, nord
  - Pixel-wise 1-px gridlines at integer scale
  - Theme reload mid-edit (watcher, `theme.reload` command, direct reload) preserves buffer; < 100 ms
  - AccessKit/kittest query of focused cell (address, value, formula, error)
  - Software-raster scroll frame < 33 ms on 1M-row sheet
- Decisions:
  - CLI remains composition root: `bootstrap_live` + `attach_session` + `TaskRunner::spawn`; GUI never owns `Mutex<Bus>`
  - Paint from `TaskRunnerHandle::snapshot()`; local commands via `apply_local_command` while busy
  - Theme from `LoadedConfig.theme.roles` only (no Omarchy parse in `gui`)
  - Fonts from `LoadedConfig.shell.ui_font_path` with egui default fallback
  - Without `WAYLAND_DISPLAY`/`DISPLAY`, fail closed (CI must not hang)
  - wgpu + `egui_kittest` software rasterizer for CI snapshots
  - One window per process for this package; extra viewports documented as follow-up if needed
- Open questions at planning time:
  1. CJK IME and Orca speech remain ADR-001 human checks (WP-S2).

## What was built

A native eframe/egui wgpu front-end over the same WP-14 session and WP-15a `TaskRunner` as the TUI. Bare `omacell [file]` is the composition root: one watched `ConfigStore`, registered file/theme/UI commands, SIGUSR1, in-process IPC, then `omacell_gui::run`. Native builds enable both Wayland and X11. The GUI starts on an empty workbook and submits the requested file through the task runner, so large-file I/O no longer blocks construction of the window or delays creation of the cancellation/progress path. The startup request is queued before IPC begins accepting commands.

The GUI never wraps `Bus` in a toolkit mutex. Paint reads `TaskRunnerHandle::snapshot()`; nav/sel/view/palette/panel go through `apply_local_command` while the writer is busy; `Esc` cancels the focused queued or running request. Task and config producers wake egui when they queue an event, including work initiated over IPC, so an idle window no longer waits for incidental input before showing a result.

The focused-IPC integration publishes eframe/winit native focus through the
retained `IpcHandle`; loss clears the marker and drop removes it. The TUI uses
the corresponding Crossterm focus-change protocol, so shell IPC defaults reach
the foreground workbook rather than whichever process started most recently.

The grid is virtualized from `Viewport` Fenwick geometry (frozen panes + the visible window) with device-pixel snapping and tessellation feathering off. It paints explicit file-authored fills, font colors/sizes, and per-edge borders with Excel-style strength precedence without retheming those colors. Chrome is sheet tabs, an expandable formula bar, status line, JSON-aware command palette, resizable docked panels, optional `[layout] menu_bar`, and a context menu. Compact width hides tabs and the menu bar. Mouse covers select/extend/add, header click, header-edge resize and double-click auto-fit, `Ctrl+scroll` zoom, and shift/horizontal scroll. The post-WP-17 integration connects copy/cut/paste, fill-handle series, atomic drag-move, and `Ctrl`-drag copy to their bus commands. Toolkit copy exports TSV while retaining the rich internal cell payload; toolkit paste imports bounded text tables through one `range.set` command.

The post-WP-18 integration consumes conditional-format overlays from the task
runner's dedicated reader worker. Frozen and scrolling panes are requested as
separate bounded rectangles; rapid viewport changes coalesce, and results are
accepted only for the exact `Arc<ReaderSnapshot>` being painted. Effective
fill/font styles, positive and negative-axis data bars, gradient bars, and 3–5
bucket icons are painted without rule parsing or formula evaluation in egui.
The TUI consumes the same cache with terminal color plus bounded bar/icon
glyphs.

Session restore reads `session.toml` (zoom, sheet, cursor, panel, recents). `eframe::App::save` writes it when eframe persistence is active, and `Drop` provides the native close fallback when it is not. Successful asynchronous open/save adopts the path and title; an opened workbook reapplies its sheet view and compatible restored session state. Tab clicks synchronize both the UI selection and the workbook's persisted active sheet. Printable in-cell input is consumed once rather than once as a key and again as committed text, and the grid no longer steals focus from the formula bar.

The file-lifecycle completion registers `file.new`, `file.close`, and
`file.saveas` at the composition root. New workbooks apply configured sheet
count/date/precision defaults, clear the old file sidecar and workbook-scoped
AI cache, and reuse the bus's `WorkbookOpened` boundary so changesets and repeat
state cannot cross workbooks. Save and save-as snapshot retained selection,
scroll, zoom, freeze, split, and show-formulas state through the existing atomic
save/backup path, preserve workbook-owned gridlines, and adopt a save-as
destination. GUI and TUI clear dirty state and reconcile
path/title/selection only after successful task completion; new, interactive
open, and close require a repeated command before discarding unsaved edits. Required-argument
key bindings now open the existing schema-driven JSON prompt, fixing both F12
save-as and Ctrl+O open instead of submitting an invalid empty payload.

Theme roles map onto egui `Visuals` and grid colors. Fonts load from `LoadedConfig.shell.ui_font_path` when the composition root asks; tests keep bundled egui fonts so snapshots are host-independent. Reload (watcher, `theme.reload`, direct `ConfigStore::reload`) rebuilds visuals without resetting the edit buffer. Malformed non-ASCII color values now fall back safely instead of panicking.

Key files:

- `crates/gui/src/{lib,app,theme,grid,chrome,input}.rs`
- `crates/gui/tests/{common,snapshots,reload,a11y,lifecycle,performance}.rs`
- `crates/gui/benches/scroll.rs`
- CLI: `cmd_gui`, `omacell [file]`, fail-closed `gui.display`
- `ConfigStore::set_event_waker` and `TaskRunnerHandle::set_event_waker`
- `deny.toml` (egui default-font OFL/Ubuntu)
- `.github/workflows/ci.yml` (`mesa-vulkan-drivers` for lavapipe)

## Interfaces exposed (for dependents)

| Item | Notes |
|---|---|
| `omacell_gui::{Launch, Gui, run}` | CLI hands over `paths`, `ConfigStore`, `Bus`, `UiSession`, `KeymapRoots`, `LongOps`, optional `file`, `use_shell_font`. `run` enables IPC. Tests use `Gui::new(launch, false, ctx)` |
| `Gui::{ui_frame, step_key, execute_cmd, poll, title, theme_name}` | kittest + event loop. `poll` drains all queued task/config events; producer wakers request the frame that performs the drain |
| `GuiTheme` / `hex_color` | Roles → egui `Visuals` and grid colors; no file parse |
| `ConfigStore::set_event_waker` | Installs the thread-safe repaint callback shared by watcher, signal, command, and direct reload paths |
| `TaskRunnerHandle::set_event_waker` | Installs the repaint callback for queued/running/progress/terminal task events and committed reader snapshots |
| Conditional-format cache | `TaskRunnerHandle::{request_conditional_formats,conditional_formats}` supplies snapshot-safe worker results; `GridLayout` returns the frozen/scrolling viewport rectangles for the next request |
| `omacell [file]` | Composition root: bootstraps an empty live app, attaches the session/SIGUSR1 adapter, then queues the optional path from `Gui::new` |
| App id `omacell` | `ViewportBuilder::with_app_id(PRODUCT_NAME)`, no SSD, title `<file> — Omacell` with `•` dirty prefix |
| `file.new` / `file.close` / `file.saveas` | Composition-owned lifecycle commands; new/save-as reconcile asynchronously, while close is interpreted as a guarded frontend control result |
| Focus publication | GUI native focus and TUI terminal focus call `IpcHandle::set_focused`; `omacell ipc` consumes the marker through default discovery |
| Clipboard/edit bridge | `ClipboardPayload` retains bus copy/cut JSON while exporting text formats; GUI toolkit events and TUI keys inject the same selection-aware command arguments |

WP-25/26/28: reuse `Launch` / `Gui`; do not load config a second time or lock `Bus` on the UI thread. Chart widgets are WP-25.

## Deviations from the spec or the package (with reasons)

- **One window per process.** Spec lists multi-window (one workbook per window). This package matches TUI/CLI: `omacell [file]` starts one instance; extra egui viewports are a follow-up. `cli.usage` rejects two files in one process.
- **No display → exit 1 (`gui.display`)**, not a stub 3. CI `assert_cmd` has no Wayland; fail-closed instead of hanging in eframe.
- **One viewport is painted.** `UiSession` retains frozen/split state, but the custom painter does not yet render multiple split panes. Complete and verify the split painter at the WP-28 GUI integration gate.
- **Per-cell typeface substitution and an explicit shaping cache are not implemented.** The resolved shell font, per-cell font size/color, and egui's own galley cache are used. WP-28 must profile and complete family/fallback fidelity on the fixed host before release.
- **Status segments are rendered but are not yet interactive.** WP-28 owns the final GUI completion/a11y pass and must bind configured clickable segments there.
- **Header resize/auto-fit write `UiSession` viewport geometry**, not sheet `ViewState`. Same split as WP-14/WP-15; persisting sizes into the file waits on save.
- **CJK IME composition UI and Orca speech** remain ADR-001 human checks. IME commit events are forwarded; `TextEdit` on the formula bar uses winit IME.
- **`epaint_default_fonts` OFL-1.1 + Ubuntu-font-1.0** are allowlisted in `deny.toml`. They are the egui bundled fallback required when `ui_font_path` is absent; not GPL-family.
- **`tempfile` 3** (dev/bench) matches WP-13/14/15 isolated HOME/theme installs.
- **`schemars` / `serde`** (gui dev-deps) register `theme.reload` in GUI tests without depending on `omacell-cli` (cycle).

## Measurements

Host: local Linux. Review artifacts and temporary files stayed under repository-local `review-scratch/`; no `/tmp` paths were used.

- `cargo test -p omacell-gui` — lib 5 + a11y 2 + lifecycle 4 + performance 1 + reload 2 + snapshots 1
- `cargo test -p omacell-cli --test subcommands` — 14 pass, including no-display `gui.display` and GUI usage rejects
- `cargo bench -p omacell-gui --bench scroll -- --quick` — `gui_scroll_1m_software` **9.27 ms** (8.99–9.34 ms) after real viewport jumps and a full wgpu render/readback on kittest's CPU-preferred adapter. This is below the 33 ms software target. The original 175.56 µs result was invalid because it repainted the same rows and never rendered them.
- `cargo test --release -p omacell-gui --test performance -- --ignored` — samples 24 positions from row 0 through row 1,048,544 after pipeline warm-up: **7.94 ms mean / 13.22 ms p95** step + software render. The nightly lane runs this optimized gate at 36.3 ms mean (33 ms + 10%), with a two-budget p95 stall guard. Required PR CI does not assert wall-clock budgets on shared runners.
- Presentation-aware first frame measured **333.67 ms** in release and **381–427 ms** in debug on the local CPU-preferred kittest adapter. GitHub's cold lavapipe initialization measured **2.58 s**. The local release result is 11.2% over the 300 ms product target, so this target is not claimed as met. The always-on GitHub test has a 5 s render-completion smoke bound, not a product-performance assertion; WP-28 explicitly owns the fixed integrated-GPU render/presentation gate and 10% baseline.
- Theme reload mid-edit gated at **< 100 ms** (`theme_reload_preserves_edit_and_is_fast`).
- File-lifecycle cleanup: `cargo test -p omacell-cli --test file_lifecycle` — 3 pass; `cargo test -p omacell-tui --test file_lifecycle` — 2 pass; `cargo test -p omacell-gui --test lifecycle` — 7 pass.
- File-lifecycle cleanup final gate: `just check` — pass (format, Clippy with warnings denied, workspace tests, and rustdoc); IPC suites were run with local Unix-socket access.
- Editing-integration follow-up: GUI lifecycle 19/19 pass, including toolkit clipboard, single/multi-cell fill handle, atomic move, and `Ctrl`-drag copy; full `just check` passes.
- Conditional-format integration follow-up: bus worker/invalidation, frozen-pane
  partitioning, GUI pixel, TUI color, data-bar/icon, and existing snapshot suites
  pass; the full `just check` gate and strict Clippy are green.
- `cargo deny check` — pass after OFL-1.1 / Ubuntu-font-1.0 allowlist

New crates.io deps: `eframe`/`egui`/`egui_kittest` 0.32 (pre-approved, wgpu + accesskit + default_fonts). Dev: `criterion`, `tempfile`, `schemars`, `serde` (justified above).

## Open questions / decisions needed

1. **Human / WP-28:** record the recommended one-process-per-file 1.0 policy in
   the release ADR, or implement extra egui viewports.
2. **Human / WP-28 G4:** CJK IME and Orca checks remain outstanding.
3. **Resolved policy and integration:** row heights and column widths are
   workbook data and use undoable commands; UI-only header chrome belongs to
   session state. Interactive save flushes the retained sheet view state.
4. **Human / WP-28 fixed host:** measure presentation-aware cold start before
   judging the 300 ms product target; the software-adapter number is diagnostic.

## RFC (only if a frozen contract changed)

Additive command-catalog change: public `file.new` and `file.close` use closed
empty-object schemas; public `file.saveas` requires `{path: string}`. All three
are mutating and not changeset eligible. No existing command id/schema, frozen
WP-01 type, event wire shape, IPC envelope, or catalog version changes. The
runtime behavior and results are recorded in `docs/contracts.md`.

## Checklist
- [x] `just check` green on a clean clone
- [x] WP-16 acceptance tests are present and green; the broader §12.1 cold-start target is explicitly handed to the fixed-host WP-28 gate
- [x] Docs warning-free; public items documented
- [x] Baselines recorded (if the package has performance gates)
- [x] No new `TODO(` without a `WP-` reference; no new dependency without justification
- [x] Nothing written outside the repository except documented temp dirs

# Report — WP-01: Core contracts: addressing, values, errors, styles, commands, changesets, events

## Plan (written before coding)

- Files/modules to create:
  - `crates/core/src/limits.rs` — `MAX_ROWS`, `MAX_COLS`, `MAX_FORMULA_LEN`.
  - `crates/core/src/error.rs` — `ErrorKind` (Excel cell errors), `CoreError { code, message, hint }`, stable `codes::*` strings.
  - `crates/core/src/addr.rs` (plus `addr/letters.rs`, `addr/a1.rs`, `addr/r1c1.rs` if the parser needs the split) — `SheetId`, `SheetSpec`, `CellRef`, `RangeRef`, column letters ↔ index, A1 and R1C1 parse/print.
  - `crates/core/src/value.rs` — `Value`, `StrId`, `ArrayId`, `Array2D`.
  - `crates/core/src/style.rs` — `Color`, `Font`, `Fill` (solid/pattern/gradient), `Border`/`BorderSide`/`BorderStyle`, `Alignment`, `Protection`, `Style`, `StyleId`, `NumFmtId`.
  - `crates/core/src/command.rs` — `CommandId`, `CommandDescriptor`, `Origin`, `Outcome`, `UndoUnit`.
  - `crates/core/src/changeset.rs` — `ChangesetId`, `ChangesetStatus`, `CommandCall`, `ChangeSummary`, `Changeset`.
  - `crates/core/src/event.rs` — `Event` enum (IPC wire format).
  - `crates/core/src/locale.rs` — `LocaleId` (Excel LCID), `LocaleSeparators`; `en-US` constants. Full locale tables stay WP-06.
  - `crates/core/src/lib.rs` — modules + selected re-exports. `product.rs` untouched except remaining the re-export.
  - `docs/contracts.md` — frozen-type list with rustdoc/source links.
  - Corpora: `tests/corpus/addr/a1.tsv`, `tests/corpus/addr/r1c1.tsv`, `tests/corpus/errors/error_type.tsv`.
  - Workspace deps in root `Cargo.toml`; `omacell-core` uses them. Path dep versions already explicit (`0.0.0`).
- Interfaces to expose (types, commands, schemas, CLI):
  - Addressing: `SheetId`, `SheetSpec { start, end: Option<String> }`, `CellRef { sheet: Option<SheetId>, row: u32, col: u16, row_abs, col_abs }`, `RangeRef` (corners + whole-row/column flags + optional 3-D `sheet`/`sheet_end`), `col_to_letters` / `col_from_letters`, `parse_a1` / `print_a1`, `parse_r1c1` / `print_r1c1` (R1C1 relative offsets resolved against a base cell).
  - Values: `Value { Empty, Number(f64), Bool, Text(StrId), Error(ErrorKind), Array(ArrayId) }`, intern handles only (`StrId`, `ArrayId`), `Array2D` shape `{ rows, cols }`.
  - Errors: `ErrorKind` with exact Excel display strings and `error_type() -> Option<u8>`; `CoreError` with `{code, message, hint}`.
  - Styles: internable records (`Eq`+`Hash`) matching F-2.4 / OOXML enumerations.
  - Commands / changesets / events: types only — no registry, no I/O, no execution (WP-07a).
  - Locale: `LocaleId(0x0409)` / separators for `en-US`. Lookup tables for other LCIDs are WP-06.
  - No CLI, no command ids registered, no schemas written under `docs/schemas/` (those are WP-07a/12).
- Tests and corpora to write first:
  - `tests/corpus/errors/error_type.tsv` — display string + `ERROR.TYPE` code (or empty = Excel `#N/A`) citing Microsoft’s ERROR.TYPE article.
  - `tests/corpus/addr/a1.tsv` and `r1c1.tsv` — hand-picked parse/print cases with a `note` column.
  - Unit: `size_of::<Value>() <= 16`; every-column A1 round-trip to `XFD`; row-limit round-trip; out-of-range → `#REF!`-class `CoreError`; `ERROR.TYPE` table.
  - `proptest`: A1 and R1C1 relative/absolute round-trips; JSON serde round-trip for every public type (finite `f64` only).
  - One rustdoc construction example per public type.
- Items the package says to "decide and document" and the decision taken:
  - **Index origin:** `row`/`col` are 0-based. A1 `A1` is `(0, 0)`; R1C1 `R1C1` is the same cell. Printers add 1. `MAX_ROWS`/`MAX_COLS` are counts (valid indices `0..MAX_*`).
  - **`CellRef.sheet`:** a resolved `SheetId` (`u32` newtype). Parsers return names in `SheetSpec`; workbook-scoped name → id is WP-02. `CellRef.sheet` is `None` for sheet-local refs.
  - **R1C1 storage:** `CellRef` stores absolute grid indices plus abs flags, as specified. Relative R1C1 (`R[-1]C[2]`) is resolved/printed against an explicit base cell. Offsets that land outside the grid are `#REF!`.
  - **Whole-row/column / 3-D:** `RangeRef` carries `whole_row` / `whole_col` so `A:A` does not print as `A1:A1048576`, plus `sheet` / `sheet_end` for `Sheet1:Sheet3!A1`.
  - **`ERROR.TYPE`:** the original package encoded the then-published 1–8 table. The 2026-08-31 follow-up adopts Excel 365's extended 1–14 table.
  - **`schemars`:** WP sketch says `schemars::RootSchema` (0.8). That type was removed in schemars 1. Freeze `arg_schema: schemars::Schema` on schemars 1.x so later command/config packages are not stuck on 0.8. Deviation noted below after implementation.
  - **JSON of `Value::Number`:** finite `f64` as a JSON number (serde_json shortest). `NaN`/`Inf` are valid in-memory IEEE values (F-2.1) but are not JSON-portable; serde tests use finite values.
  - **`Event`:** `#[non_exhaustive]` because the WP lists `…`. Other enums are exhaustive so adding a variant after G0 is a visible freeze break.
  - **`smallvec`:** allowed; used for gradient stops (typically 2). Not used to replace `Vec<CommandCall>` (WP sketch freezes `Vec`).
  - **Style `Eq`/`Hash`:** `f64` fields (`font.size_pt`, gradient/theme tints) compare by `to_bits` so WP-02 can intern by value.
- Open questions at planning time:
  1. Resolved 2026-08-31: use Excel 365's extended 1–14 `ERROR.TYPE` table.
  2. Should `Event` grow `SheetRenamed` / `SelectionChanged` / `ConfigReloaded` now, or wait for WP-07a/12? Plan: only the WP-listed variants plus F-10.1 `on_open` / `on_before_save` / changeset applied/reverted.
  3. External-workbook syntax `[Book.xlsx]Sheet1!A1` — parse in WP-01 or leave to WP-03? Plan: reject with `addr.parse` here; WP-03 owns formula-level external refs (F-1.5).

### 2026-09-03 boolean-sheet-name plan (written before coding)

- Add A1 corpus rows first for `TRUE` and mixed-case `FALSE` sheet names,
  requiring their canonical reference text to quote the sheet qualifier so it
  cannot be interpreted as a formula boolean.
- Make the shared sheet-name quoting predicate recognize boolean keywords,
  thereby fixing every address-producing caller, and remove the formula
  printer's duplicate special case.
- Preserve every public type and function signature, accepted input syntax,
  and ordinary sheet-name output; add no dependency. Run the address and
  formula suites, complete core tests and doctests, strict core Clippy, the
  exact `just check` gate, and repository lint.

### 2026-09-03 formula-length error-code plan (written before coding)

- Add a core regression first that sends the same oversized formula through
  the parser and formula interner and requires both failures to use the single
  pre-existing WP-01 code, `formula.len`.
- Make the WP-03 `formula::codes::LENGTH` constant an alias of the frozen
  `error::codes::FORMULA_LEN` constant. Update the invalid corpus and public
  documentation so they no longer advertise the duplicate `formula.length`
  wire value.
- Preserve both public constant names, all error types and signatures, the
  8,192-byte limit, offsets, and hints. Add no dependency; run the formula and
  core suites, strict core Clippy, the exact `just check` gate, and repository
  lint.

## What was built

Frozen `omacell-core` types for addressing, values, Excel errors, styles, commands, changesets, events, limits, and locale identity. No I/O, toolkit, or async. `product.rs` is unchanged.

The 2026-09-03 boolean-sheet-name follow-up makes the shared
`quote_sheet_name` contract produce unambiguous qualifiers for `TRUE` and
`FALSE`, case-insensitively. All address-producing consumers now share that
behavior, and the formula printer no longer carries a private duplicate.

The 2026-09-03 formula-length error-code follow-up makes the formula parser
and formula interner report the same stable `formula.len` code for the shared
8,192-byte limit. The public `formula::codes::LENGTH` name remains available
as an alias of `error::codes::FORMULA_LEN`, and the corpus and contract
documentation now describe the single wire value.

Key files:

- `crates/core/src/{addr,value,style,command,changeset,event,error,limits,locale}.rs` and `addr/{letters,a1,r1c1,scan}.rs`
- `docs/contracts.md` — freeze list with source/rustdoc links
- Corpora: `tests/corpus/addr/a1.tsv`, `tests/corpus/addr/r1c1.tsv`, `tests/corpus/errors/error_type.tsv`

Key tests:

- `crates/core/tests/contracts.rs` — A1/R1C1 properties, every-column/every-row round-trip, out-of-range `#REF!`, validated wire invariants, changeset inverse lifecycle, serde of every public type, corpora
- `crates/core/src/value.rs` — `size_of::<Value>() <= 16` (also a compile-time `const` assert)
- `crates/core/src/error.rs` — display strings and `ERROR.TYPE` corpus
- `crates/core/src/addr/letters.rs` — every column A..=XFD
- `tests/corpus/addr/a1.tsv` — boolean-keyword single-sheet and 3-D qualifier
  regressions
- `crates/core/tests/formula.rs` — parser/interner oversized-input code parity
- rustdoc example per public type (50 doctests)

## Interfaces exposed (for dependents)

Canonical paths (also listed in `docs/contracts.md`):

| Module | Types / fns |
|---|---|
| `omacell_core::limits` | `MAX_ROWS`, `MAX_COLS`, `MAX_FORMULA_LEN` |
| `omacell_core::addr` | `SheetId`, `SheetSpec`, `CellRef`, `RangeRef`, `ParsedRef`, `RefKind`, `col_to_letters`, `col_from_letters`, `parse_a1`, `parse_a1_cell`, `parse_r1c1`, `parse_r1c1_cell`, `quote_sheet_name` |
| `omacell_core::value` | `Value`, `StrId`, `ArrayId`, `Array2D` |
| `omacell_core::error` | `ErrorKind`, `CoreError`, `codes::*` |
| `omacell_core::style` | `Style`, `StyleId`, `NumFmtId`, `Font`, `Fill`, `Border`, `Alignment`, `Protection`, `Color`, and OOXML enumerations |
| `omacell_core::command` | `CommandId`, `CommandDescriptor`, `Origin`, `Outcome`, `UndoUnit`, `UndoUnitId` |
| `omacell_core::changeset` | `Changeset`, `ChangesetId`, `ChangesetStatus`, `CommandCall`, `ChangeSummary` |
| `omacell_core::event` | `Event` |
| `omacell_core::locale` | `LocaleId`, `LocaleSeparators` |
| `omacell_core::product` | `PRODUCT_NAME`, `PRODUCT_DISPLAY_NAME` (unchanged) |

No command ids are registered (WP-07a). No CLI flags added.

Reusable corpora: `tests/corpus/addr/*.tsv`, `tests/corpus/errors/error_type.tsv`.

Index origin: 0-based. `CellRef.sheet` is a resolved id; full parsers return names in `SheetSpec`, while cell-only parsers reject sheet-qualified input. R1C1 relatives resolve against a validated base cell. Invalid `CellRef`/`Array2D` wire values are rejected.

Changeset lifecycle: proposed changesets have an empty inverse list. WP-07a computes inverses from trusted workbook state before marking them applied; applied/reverted non-empty changesets require a non-empty inverse list. Agent-provided inverses are never trusted.

## Deviations from the spec or the package (with reasons)

- **`CommandDescriptor.arg_schema` is `schemars::Schema`, not `RootSchema`.** schemars 1.2.2 removed `RootSchema`. Freezing 1.x avoids pinning later command/config packages to 0.8.
- **`Event` includes F-10.1 `WorkbookOpened` / `BeforeSave` and changeset applied/reverted**, in addition to the WP sketch list, and is `#[non_exhaustive]` because the WP writes `…`.
- **`ParsedRef` / `SheetSpec` / `RefKind`** are extra types so A1/R1C1 parsers can return sheet *names* without a workbook id map (WP-02).
- **`locale` is identity + `en-US` separators only.** Full LCID tables are WP-06.
- **JSON `Value::Number` is not bit-exact** for arbitrary `f64` (serde_json shortest). In-memory IEEE values including NaN/Inf are allowed. Serde tests use integer-valued floats plus a few exact decimals.
- **`UndoUnitId`** sits next to the `UndoUnit` marker so WP-02 has a handle type.
- **Resolved — boolean-keyword sheet names.** `TRUE` and `FALSE` are valid
  worksheet names but bare qualifiers collide with formula boolean tokens.
  The shared quoting contract now handles them case-insensitively instead of
  relying on the formula printer's local exception.
- **Resolved — formula length code.** `formula.len` predates the formula
  parser and is the frozen WP-01 engine code used by the formula interner.
  The later parser-specific `formula.length` value duplicated the same
  condition, so `formula::codes::LENGTH` now aliases the WP-01 constant.

## Measurements

No performance gates in this package.

- `just check` — pass (fmt, clippy `-D warnings`, `cargo test --workspace`, `cargo doc --workspace --no-deps`). Also `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --no-deps` — pass.
- `cargo test -p omacell-core` — 8 unit + 19 integration + 50 doctests, all pass. Integration includes a full-grid A1 round-trip (`0..MAX_COLS` and `0..MAX_ROWS` on columns A and XFD), ~3.1 s debug.
- `cargo deny check` — pass (advisories, bans, licenses, sources). Unused-allowlist warnings only.
- `size_of::<Value>() <= 16` — compile-time `const` assert in `value.rs` plus unit and integration tests.
- Corpora: 30 A1 rows, 19 R1C1 rows, 14 error-type rows (every `ErrorKind`).
- 2026-09-03 boolean-sheet-name follow-up: the three new A1 rows initially
  reproduced `TRUE!A1` instead of the required `'TRUE'!A1`. After centralizing
  keyword detection, all **33** A1 rows and all 18 formula tests pass,
  including mixed-case and 3-D qualifiers. The complete `omacell-core` suite
  passes with 103 doctests, and strict all-target core Clippy is warning-free.
  The exact `just check` gate passes with formatting, workspace-wide
  warnings-as-errors Clippy, all tests and doctests (including available
  external-tool cross-checks), and warning-free workspace documentation;
  dedicated repository lint passes 10/10.
- 2026-09-03 formula-length error-code follow-up: the direct parity regression
  initially observed `formula.length` from parsing and `formula.len` from
  interning the same oversized source. After aliasing the parser constant, all
  19 formula tests and the complete `omacell-core` suite pass, including 103
  doctests; strict all-target core Clippy is warning-free. The formula invalid
  corpus, its README, the WP-03 interface report, and the frozen-contract code
  list now consistently expose `formula.len`. The exact `just check` gate
  passes with formatting, workspace-wide warnings-as-errors Clippy, all tests
  and doctests (including real LibreOffice cross-checks), and warning-free
  workspace documentation; dedicated repository lint passes 10/10.

Locked versions: serde 1.0.229, serde_json 1.0.151, schemars 1.2.2, thiserror 2.0.20, smallvec 1.15.2, proptest 1.11.0 (dev).

## Open questions / decisions needed

1. **Resolved 2026-08-31:** `ERROR.TYPE` uses the Excel 365 extended table through 14.
2. **Resolved:** WP-03 owns external-workbook formula syntax; address-only
   `addr.parse` correctly rejects it because the engine has no multi-workbook
   navigation surface.
3. **Resolved:** every `MAX_FORMULA_LEN` rejection uses `formula.len`; the
   parser-specific public constant is an alias, not a second wire code.

## RFC (only if a frozen contract changed)

None. G0 has not happened; this package creates the freeze.

**Post-freeze semantic correction (2026-08-31).** `ErrorKind` and its serialized
shape are unchanged, but `error_type()` now returns Excel 365's extended codes
9–14 for the six newer error variants. The repository owner supplied the
decision and explicitly requested the applicable fix, satisfying the human
approval requirement for this behavioral contract correction.

**Post-freeze quoting correction (2026-09-03).** The `quote_sheet_name`
signature and every frozen address type remain unchanged. Its output now
single-quotes case-insensitive `TRUE` and `FALSE` sheet names so all generated
references are formula-safe. The repository owner requested implementation of
the audit findings and authorized the fixes and green merges, satisfying the
required human approval.

**Post-freeze formula-length code correction (2026-09-03).** Both public
constant names and all error types and signatures remain unchanged, but the
later `formula::codes::LENGTH` value changes from the duplicate
`formula.length` string to the pre-existing frozen `formula.len` wire code.
The repository owner requested implementation of the audit findings and
authorized the fixes and green merges, satisfying the required human approval
for this stable-code correction.

## Checklist

- [x] `just check` green locally (see Measurements)
- [x] Every acceptance criterion ticked with evidence (see below)
- [x] Docs warning-free (`RUSTDOCFLAGS="-D warnings"`); public items documented (50 doctests)
- [x] Baselines recorded (if the package has performance gates) — n/a
- [x] No new `TODO(` without a `WP-` reference; no new dependency without justification (all pre-approved in AGENTS.md; `cargo deny check` green)
- [x] Nothing written outside the repository except documented temp dirs

### Acceptance (WP-01)

- [x] Property tests: A1 ↔ (row, col) round-trips for every column to `XFD` and every row to the limit; out-of-range addresses are `#REF!`-class (`codes::ADDR_REF`); R1C1 relative/absolute round-trips (`crates/core/tests/contracts.rs`)
- [x] Error display strings match Excel exactly; the Excel 365 `ERROR.TYPE` table 1–14 is asserted against `tests/corpus/errors/error_type.tsv`
- [x] `size_of::<Value>() <= 16` asserted (compile-time and tests)
- [x] All public items documented; `cargo doc` has no warnings; serde round-trip for every public type

# Report — WP-18: Sort, AutoFilter, tables, data validation, conditional formatting (data tools II)

## Plan (written before coding)

- Add `sort`, `filter`, `validation`, `condfmt`, and `flashfill` core modules; extend tables and workbook mutation/undo state.
- Expose typed core APIs and public bus commands for sort, filters, tables, validation, conditional formatting, and Flash Fill.
- Write on-disk sort/filter/DV/CF corpora first, plus exact undo, `.xlsx` round-trip, LibreOffice, and 100k-cell CF performance tests.
- Preserve unchanged imported OOXML fragments; emit modeled OOXML after a definition is edited or cleared.
- Move cells as units during sort and rewrite relative references by the move delta. Keep slicers Tier 1.
- Use min/mid/max interpolation for color scales and percentile buckets for icons; record differences from Excel where exact algorithms are not specified.

## What was built

- `crates/core/src/sort.rs`: stable multi-key row/column sorting by value, fill, font, resolved icon, or custom list; header auto-detection; case sensitivity; blanks last in both directions; strict key bounds; hidden rows/columns remain in place; relative formulas move with cells.
- `crates/core/src/filter.rs`: saved AutoFilter state, exact/text/numeric/date/color/top-N/average criteria, case-insensitive dropdown search through `filter_value_options`, filter-owned hidden-row tracking, imported-state restoration, toggle and clear.
- `crates/core/src/tables.rs` and `workbook.rs`: non-overlapping create/resize/convert, duplicate-header normalization, theme table-style name, totals functions, calculated-column fill, adjacent-entry expansion, exact changeset restore, and structured-reference updates on rename.
- `crates/core/src/validation.rs`: whole/decimal/list/date/time/text-length/custom rules, Unicode-scalar text length, relative formulas, inline/range dropdown resolution, bounded invalid-cell query, and registry-aware formula evaluation.
- `crates/core/src/condfmt.rs`: cell/formula/text/date/blank/error/duplicate/unique/top-bottom/average rules, 2/3-color scales, data bars with negative axis, 3–5 icon buckets, property-wise priority and stop-if-true, theme defaults, registry-aware formulas, and a bounded resolved-overlay cache.
- `crates/core/src/flashfill.rs`: deterministic first/last-word, prefix/suffix, surround, and constant patterns.
- `crates/bus/src/data.rs` and `restore.rs`: typed public commands, strict enum/operator validation, exact changeset inverses for table operations, and theme-role defaults for new scales/bars.
- `crates/io/src/xlsx/{read,data,write}.rs`: modeled AutoFilter/DV/CF/table parse/write, correct nested OOXML, active-filter `sheetPr@filterMode`, full raw AutoFilter preservation, raw-extras preservation only while the loaded model is unchanged, and safe preservation of definitions rejected by the strict model.
- `crates/io/src/omc/{read,write}.rs`: modeled filter/DV/CF records so `.xlsx → .omc → .xlsx` retains both semantics and eligible raw extras.
- `tests/corpus/data-tools/*.tsv`: committed, executed corpora for all four acceptance surfaces.

## Interfaces exposed (for dependents)

Core:

- Sort: `SortSpec`, `SortKey`, `SortBy::{Value,FillColor,FontColor,Icon}`, `detect_header`, `sort_range`.
- Filter: `AutoFilter`, `FilterColumn`, `FilterCriteria`, `apply_filter`, `restore_filter`, `clear_filter`, `toggle_filter`, `filter_value_options`, `MAX_FILTER_VALUE_OPTIONS`.
- Tables: `Workbook::{create_table,resize_table,convert_table,restore_table,rename_table,set_table_totals}`; `TableColumn::totals_fn`; `Table::style_name`.
- Validation: `DataValidation`, `validate_cell`, `validate_cell_with_registry`, `invalid_cells`, `invalid_cells_with_registry`, `validation_list_values`, `MAX_INVALID_CIRCLES`, `MAX_VALIDATION_LIST_ITEMS`.
- Conditional formatting: `CondFormat`, `CfKind`, `CfTimePeriod`, `CfDxf`, `CfOverlay`, `CfVisual`, `OverlaySource`, `ResolvedCfOverlay`, `overlay_at`, `overlay_at_with_registry`, `overlay_range`, `overlay_range_with_registry`, `resolve_overlay`, `resolve_overlay_with_registry`, `MAX_CF_OVERLAY_CELLS`.
- Flash Fill: `flash_fill`.

WP-28 should build overlays on a worker with `resolve_overlay_with_registry(workbook, sheet, visible_range, engine.registry())`, then paint from `ResolvedCfOverlay::get`; this keeps formula parsing and rule evaluation off the UI thread while retaining the registered function library.

Public commands:

| Id | Important arguments / result |
|---|---|
| `range.sort` | `range`, ordered `keys`, nullable `header` (omitted = detect), `case_sensitive`, `left_to_right` |
| `filter.toggle` | `range` |
| `filter.clear` | none |
| `filter.set` | `range`, typed per-column criteria |
| `filter.values` | `range`, `col_id`, `search`; returns bounded distinct display values |
| `table.create` | `range`, `name` |
| `table.resize` | `id` or `name`, `range` |
| `table.convert` | `id` or `name` |
| `table.rename` | `id` or `name`, `new_name` |
| `table.totals` | `id` or `name`, `show`, per-column `functions` |
| `validation.set` | `range`, kind/operator/formulas and message/error fields |
| `condfmt.add` | range/kind/formulas/priority/style plus top-N, scale, bar, icon, and date-period options |
| `edit.flashfill` | `range` |

All mutating commands are changeset-eligible. `filter.values` is a query.

## Deviations from the spec or the package (with reasons)

- Slicers remain Tier 1, as WP-18 explicitly requires.
- Notes/comments do not follow sorted cells. Formula cells do follow and use `RewriteOp::Copy { drow, dcol }`; this is documented in `docs/compat/known-differences.md`.
- Color scales interpolate linearly across the observed numeric min/mid/max, and icon sets use rank buckets. Excel has additional threshold types and percentile edge behavior that are not modeled here.
- Flash Fill intentionally supports a deterministic pattern subset rather than Excel's full inference engine.
- SpreadsheetML does not identify whether a hidden row was hidden manually or by AutoFilter. On import, a hidden row that fails the saved criteria is treated as filter-owned; a hidden row that passes remains manual.
- Raw unsupported/invalid CF, DV, and AutoFilter XML is preserved while its successfully loaded model remains unchanged. Omacell does not expose unmodeled fields for editing.

## Measurements

- Committed corpus rows: sort 3, filter 4, validation 5, conditional formatting 4. They execute from `crates/core/tests/data_tools.rs`; the full integration target contains 29 passing tests.
- Bus data target: 9 passing tests, including strict schemas, header detection, dropdown search, exact sort/table changeset inversion, rename, totals, and visual-rule construction.
- `.xlsx`: `wp18_all_modeled_data_definitions_roundtrip`, `wp18_edits_override_imported_definition_fragments`, the corpus-wide L3 round-trip, and `.omc` bridge tests pass.
- LibreOffice: `cargo test -p omacell-io --test xlsx_roundtrip wp18_modeled_filter_dv_cf_roundtrip -- --exact --nocapture` passed in 0.84 s on LibreOffice 26.2 and verified AutoFilter, DV, CF, and table/totals definitions after headless conversion. The generated worksheet explicitly marks active filter mode for compatibility with the LibreOffice 24.2 exporter used by GitHub CI.
- Performance: `cargo test -p omacell-core --test data_tools cf_eval_100k_cells_20_rules_is_under_100ms --release -- --exact --nocapture` passed; the entire test completed in 0.07 s and the timed 100k-cell overlay assertion was below 100 ms after one edit.
- Criterion baseline remains `crates/core/benches/condfmt.rs::overlay_100k_20_rules_after_edit`.

## Open questions / decisions needed

- Human approval of the RFC below is required before merge. No technical acceptance item remains open.

## RFC (frozen contracts changed — human approval required)

### Requested changes

- Extend the frozen public `Sheet` model with AutoFilter, validation, and conditional-format collections. The filter-owned hidden-row set stays crate-private. `SheetEditState` retains its existing `Eq` contract and now serializes these additions for exact undo/changesets.
- Extend frozen `TableColumn` with optional `totals_fn` and `Table` with defaulted `style_name`. Existing serialized tables migrate through serde defaults; Rust struct literals must add the new fields or use constructors.
- Add the WP-18 core modules, types, and methods listed above. Existing method signatures and `Event` variants are unchanged.
- Add public WP-18 command schemas to the frozen command catalog. `range.sort.header` is nullable so omission means header detection; explicit `true`/`false` remains deterministic. No IPC envelope field or version changes.
- Extend public `WorksheetExtras` with `autofilter_xml` and add `.omc` data-tool model records. Existing `.omc` files remain readable; new readers reject malformed model JSON rather than silently losing definitions.

### Why

The WP-18 state must participate in the workbook, undo/changeset snapshots, public command surface, and file-preservation layer. Keeping it in side maps would make edits non-atomic, break exact revert, and make `.xlsx` definitions disappear after legitimate edits.

### Migration and compatibility

- Serialized core fields use empty/default values, so older data loads without migration code.
- Downstream Rust code using `Table`/`TableColumn`/`WorksheetExtras` struct literals must add the new fields; constructor-based callers are unchanged.
- Command consumers should regenerate `commands.json`; existing command payloads and the IPC v1 envelope are unchanged.
- Older Omacell builds will not understand the new `.omc` model-record kinds. `.xlsx` remains the cross-version interchange format.

### Alternatives considered

- Sidecar-only state was rejected because it cannot provide exact undo, changeset inversion, or modeled save-after-edit semantics.
- Always letting raw extras win was rejected because clearing or editing a definition would silently resurrect the imported XML.
- Always regenerating raw definitions was rejected because unsupported OOXML details would be lost.

### Approval gate

PR #36 must not merge until a human approves these frozen-contract changes.

## Checklist

- [x] `just check` green on the review clone (fmt, clippy `-D warnings`, workspace tests, doctests, and rustdoc)
- [x] Every acceptance criterion ticked with evidence above
- [x] Docs warning-free; public items documented
- [x] CF performance baseline recorded and release gate passed
- [x] No new `TODO(` without a `WP-` reference; no new dependency
- [x] All new scratch, LibreOffice profiles, and build output stayed under the repository review directory

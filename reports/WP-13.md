# Report — WP-13: CLI: the `omacell` binary

## Plan (written before coding)

### CLI and composition hardening follow-up (2026-09-04)

- Add regressions first for invalid configuration left by `$EDITOR`, XDG-aware
  help text, backup/peer-lock behavior for CSV/TSV/OMC saves, private log modes,
  child-process output containment, setup preflight atomicity, conservative
  Omarchy uninstall dispatch, library output routing, and private MCP socket
  parents.
- Validate the complete retained `LoadOptions` after `config edit` succeeds;
  leave the edited file in place for correction but return an operational error
  instead of reporting success for invalid content.
- Route CSV, TSV, and OMC workbook saves through the shared peer-lock-aware
  atomic writer with configured backup rotation, matching the other writable
  formats.
- Create log directories/files as 0700/0600 and repair existing log-file mode
  before rotation. Null inherited child stdout/stderr where helpers are not
  protocol transports so JSON, IPC, and MCP streams stay uncontaminated.
- Require a private, owned 0700 parent for explicit MCP Unix sockets before
  bind. The ordinary IPC server already binds only under its validated private
  runtime directory.
- Check Hyprland input before setup writes, expose `setup omarchy --uninstall`
  over WP-12's conservative library API, and make dry-run cover both directions.
  Route broadcast status through `Output` rather than printing from the library.
- Correct global/config/theme/format help and generated docs for XDG paths and
  the actual role-overlay theme format. Keep frozen command/event/IPC contracts
  unchanged, add no dependency, then run focused suites and exact `just check`.

- Files/modules to create:
  - `crates/cli/src/{lib,cli,error,output,log,app,files,reload,run}.rs` — clap tree, composition root, file/theme adapters, SIGUSR1
  - `crates/cli/src/main.rs` — thin process-exit entry
  - `crates/cli/tests/{help,subcommands,config,reload,dry_run,dist}.rs`
  - `docs/cli.md` — exit codes and command surface
  - `fuzz/fuzz_targets/cli_args.rs` — clap parse smoke
  - Narrow conf helpers: `reset_user_rel`, `ReloadHandle` (Sync reload for commands/SIGUSR1)
  - Bus: `CommandContext::is_dry_run` so `file.save`/`file.export` skip disk on dry-run/propose scratch
- Interfaces to expose (types, commands, schemas, CLI):
  - Binary `omacell` with F-10.5 command tree
  - Registered commands `file.open`, `file.save`, `file.export`, `theme.reload` (public, not changeset-eligible)
  - `omacell_cli::{command, run, register_file_commands, register_theme_reload, spawn_sigusr1_reloader}` for tests and WP-15/16
  - Completions (bash/zsh/fish) and man page written to `target/dist/` during tests
- Tests and corpora to write first:
  - `assert_cmd` for every subcommand including stubs (`run`, `ai`, `agent`, `mcp`, `audit`, `--tui`)
  - `--json` usage/operational error shape `{code,message,hint}` and `--dry-run` no-write proofs, including IPC
  - Help snapshots (`insta`) per command
  - Config: `--config`, `--theme`, env `OMACELL_*`, repeated `--set`, workbook overlay via `--from-workbook`
  - `theme.reload` local, IPC `--all`, SIGUSR1 adapter; no IPC schema change
  - Arg fuzz target listed by `cargo fuzz list`
- Items the package says to "decide and document" and the decision taken:
  - CLI origin is `Origin::User` (no new frozen origin).
  - Named `config reset [file]` is a conf helper that stays under `Paths::user_config`.
  - `theme.reload` is a registry command, not a `ControlOp`.
  - SIGUSR1 uses `signal-hook` (safe flag; crate forbids `unsafe`). Justified below.
  - File adapters live in `crates/cli` (io ↛ bus).
  - `--tui` / bare files dispatch stubs until WP-15/16; exit non-zero with a hint.
  - `audit` / `run` / `ai` / `agent` / `mcp` stubs exit non-zero with a WP hint.
- Open questions at planning time:
  1. Should `omacell ipc` hide `edit.undo` / `edit.redo`? Left as pass-through (socket already allows execute).
  2. Sparse TOML write for `config edit` is `$EDITOR`; `--dry-run` prints the path.

## What was built

The `omacell` binary is a clap tree over one composition root: one `Paths`, one retained `LoadOptions`, one `ConfigStore` (`load_with`, no watcher on one-shots), one `Bus` with WP-07a core commands plus `file.open`/`file.save`/`file.export` and `theme.reload`. File I/O stays in `omacell-io`; the CLI only registers adapters. SIGUSR1, IPC `--all`, and `theme.reload` share `ReloadHandle`, and direct reloads publish the same config events as filesystem reloads. External command effects run once after bus preflight; user dry-runs validate serialization without writing or changing sidecars.

The post-WP-16 focus integration makes bare `omacell ipc` select the focused
GUI/TUI instance, with the original newest-live discovery as a startup
fallback. Broadcast `--all` targeting is unchanged.

The 2026-09-04 composition review now validates the complete retained config
after `$EDITOR` returns; routes CSV, TSV, and OMC replacement through the same
peer-lock and backup policy as other writable formats; and confines log files,
explicit MCP socket parents, and non-protocol child process streams. Omarchy
setup checks Hyprland before writing and exposes the conservative WP-12
uninstall API. Human broadcast status now goes through `Output`, so library
dispatch no longer writes directly to stdout. The clap snapshots and generated
reference state the real XDG locations, role-overlay theme format, and complete
conversion formats.

Key files:

- `crates/cli/src/{lib,main,cli,error,output,log,app,files,reload,run}.rs`
- `crates/cli/tests/{help,subcommands,config,reload,dry_run,save_policy,mcp,changeset,dist,version,repo_lint}.rs`
- `crates/cli/tests/snapshots/help__*.snap`
- `docs/cli.md`
- `fuzz/fuzz_targets/cli_args.rs`
- Conf: `reset_user_rel`, `validate_user_rel`, `ConfigStore::load_with`, `ReloadHandle::{check,reload}`
- Bus: `CommandContext::{is_preflight,is_dry_run}`

## Interfaces exposed (for dependents)

| Item | Notes |
|---|---|
| `omacell` binary | F-10.5 tree; global `--json` `--dry-run` `--set` `--config` `--theme` `--from-workbook` |
| `convert --plan plan.json` | bounded and validated WP-08 `ImportPlan` JSON for CSV input |
| `omacell_cli::{command, try_parse, run, write_dist}` | clap Command, argv parse, process runner, dist artifacts |
| `register_file_commands` / `FileSession` | `file.open`, `file.save`, `file.export` |
| `register_theme_reload` / `spawn_sigusr1_reloader` | `theme.reload` + SIGUSR1 → `ReloadHandle::reload` |
| `conf::{reset_user_rel, validate_user_rel, ConfigStore::load_with, ReloadHandle}` | contained named reset, one-shot load/check, event-producing Sync reload |
| `CommandContext::{is_preflight,is_dry_run}` | external adapters distinguish bus preflight from a user-requested dry-run |
| Exit codes | `docs/cli.md`: 0 success, 1 operational, 2 usage. The historical stub-only exit 3 was removed after all CLI packages landed. |
| Completions / man | `target/dist/` via `cargo test -p omacell-cli --test dist` |
| `setup omarchy --uninstall` | Removes only unchanged Omacell-owned assets and owned menu rows; dry-run performs no writes. |
| Explicit MCP socket policy | Parent is created private or must already be current-user-owned mode 0700; socket is mode 0600. |

WP-15/16: reuse `register_theme_reload`, `spawn_sigusr1_reloader`, and one `ConfigStore` from this composition root. Do not add a `ControlOp`.

## Deviations from the spec or the package (with reasons)

- **`signal-hook` 0.3** is not on the pre-approved list. It is the safe, unregistering SIGUSR1 adapter (`flag::register`); this crate forbids `unsafe`. License Apache-2.0/MIT. Drop when WP-15 can own the long-running loop.
- **`predicates` 3** is not on the pre-approved list. It supplies readable `assert_cmd` output matchers for the CLI contract suite. License Apache-2.0/MIT.
- **`tempfile` 3** is not on the pre-approved list. It supplies isolated managed homes/runtime directories for no-write, IPC, and symlink-containment tests. License Apache-2.0/MIT.
- **One-shot CLI uses `ConfigStore::load_with`**, not `load_and_watch_with`. Package default `live_reload = true` would otherwise start a notify watcher for `fn list`. Reload still works via `ReloadHandle`. TUI/GUI should call `load_and_watch_with`.
- **`--from-workbook`** is extra so `config show` can prove the settings overlay in a one-shot process. Query/set/eval/recalc always overlay the opened book.
- **`edit.undo` / `edit.redo`** remain legal on `omacell ipc` (WP-07b already allows execute).
- **Resolved frontend dispatch:** at the WP-13 landing, bare `omacell [file]` / `--tui` used the temporary exit-3 stubs. WP-15/16 now launch the retained TUI/GUI, and the unused stub exit code and constructor have been removed.
- **Explicit MCP socket parents are strict:** a caller-provided parent with a
  mode other than 0700 is rejected instead of silently chmodded. This avoids
  a bind-before-chmod exposure and never changes permissions on a directory
  the caller may share for another purpose.

## Measurements

Host: local Linux.

- `just check` — pass (fmt, clippy `-D warnings`, `cargo test --workspace`, `cargo doc --workspace --no-deps`).
- Focused composition regression suite — pass: 50 checks across the CLI library,
  config, dry-run, help, subcommands, and save-policy targets; the 3-test MCP
  integration target also passes with private socket fixtures.
- `ipc_focus.rs` black-box coverage proves default routing reaches a focused
  older instance instead of a synthetically newer live socket.
- `cargo deny check` — advisories, bans, licenses, sources pass.
- `cargo audit` — no vulnerability; the two repository-allowed unmaintained
  transitive warnings remain (`paste`, `ttf-parser`).
- `cargo +nightly fuzz list` includes `cli_args`.
- Criterion: none (no §12.1 CLI gate).

New crates.io deps (pre-approved unless noted): `clap`, `clap_complete`, `clap_mangen`, `tracing-subscriber`, `assert_cmd`, `insta`; `predicates`, `tempfile`, and `signal-hook` are justified above. The unused `anyhow` dependency was removed during review.

## Open questions / decisions needed

1. **Resolved:** WP-15/16 use the CLI composition root, including IPC, signal,
   and one `ConfigStore`.
2. **Resolved:** same-user IPC may use undo/redo; agent origins remain
   changeset-only.

## RFC (only if a frozen contract changed)

None. `core::Event` and the WP-07b IPC envelope are unchanged. `theme.reload` is a registry command.

## Checklist
- [x] `just check` green on a clean clone
- [x] Every acceptance criterion ticked with evidence
- [x] Docs warning-free; public items documented
- [x] Baselines recorded (if the package has performance gates) — n/a
- [x] No new `TODO(` without a `WP-` reference; non-pre-approved test/signal dependencies justified above; `cargo deny check` green
- [x] Nothing written outside the repository except documented temp dirs

### Acceptance (WP-13)

- [x] `assert_cmd` integration tests for every subcommand incl. JSON usage/operational errors, the full changeset lifecycle, `convert --plan`, and `--dry-run` proving no file/session changes — `subcommands.rs`, `changeset.rs`, `dry_run.rs`
- [x] Help snapshots (`insta`) for every command and leaf subcommand; exact bash/zsh/fish/man artifacts generated in CI — `help.rs`, `dist.rs` (`target/dist/`)
- [x] Exit codes documented and asserted; malformed input never panics (fuzz smoke on args) — `docs/cli.md`, `version_and_usage`, `fuzz/fuzz_targets/cli_args.rs`
- [x] Configuration integration tests prove `--config`, `--theme`, workbook settings, env and repeated `--set` reach one retained `LoadOptions`; reset recovers invalid config without symlink escape; `theme.reload` works locally, over IPC `--all`, and through the SIGUSR1 adapter with reload events and no IPC schema change — `config.rs`, `reload.rs`, `conf/tests/layering.rs`

# Report — WP-21: MCP server, agent skill, and Omarchy agent hand-off

## Plan (written before coding)
- Files/modules to create:
  - `crates/bus/src/mcp/{mod,catalog,session,uri}.rs` — frozen tool catalog, resource URIs, sync `McpSession` over `&mut Bus`
  - `crates/bus/tests/mcp.rs` — every tool/resource, pagination, errors, propose-not-mutate
  - `crates/cli/src/{mcp,agent}.rs` — `rmcp` stdio/`--socket` server (tokio), `omacell agent` / `diagnose`
  - `crates/conf/src/{agent,notify}.rs` — default-agent detection, `omarchy agent prompt` argv, `omarchy-notification-send` / freedesktop notify
  - `default/agents/skills/omacell/SKILL.md` — replace WP-21 placeholder
  - `docs/mcp.md`, `docs/schemas/mcp.schema.json` — generated from the catalog
  - `fuzz/fuzz_targets/mcp_json.rs` — bounded tool-argument JSON
  - Tests: `crates/cli/tests/{mcp,agent,skill}.rs`; setup symlink + idempotent in `crates/conf/tests/setup.rs`
- Interfaces to expose (types, commands, schemas, CLI):
  - MCP tools and `omacell://{file}/card` + `omacell://{file}/{sheet}` resources (schema version 1, freeze with this package)
  - CLI: `omacell mcp [--socket] [--book]`, `omacell agent [--book] [--selection] <prompt>`, `omacell agent diagnose [--pid] [--book]`, `omacell recalc --wait`
  - Command `ai.agent` (palette/status; hidden when no default agent)
  - `omacell_conf::{detect_default_agent, hand_off, notify_send}`
- Tests and corpora to write first:
  - In-process MCP session vs a fixture workbook (every tool/resource, pagination, errors)
  - `rmcp` client against `omacell mcp` (stdio) including resources
  - Skill-drift vs `omacell --help`
  - Fake `omarchy` on `PATH` (args, cwd, hidden without default agent)
  - Headless MCP proposal visible to `omacell changeset list` and CLI apply/revert
  - Setup: five symlinks resolve, idempotent rerun, drift on the linked file
- Items the package says to "decide and document" and the decision taken:
  - **I/O vs bus:** MCP catalog and dispatch live in `omacell-bus` (sync). `rmcp` + tokio live in `omacell-cli` so GUI/TUI do not compile an async runtime. File tools call registered `file.open`/`file.save`/`file.export` (CLI composition root).
  - **Headless changeset list:** `omacell mcp` also binds the WP-07b IPC socket, so `omacell changeset list|apply|revert` talks to the same `Bus`.
  - **Origins:** grid writes use `Origin::ExternalAgent` and default to `propose`. `apply=true` / `changeset_apply` attempt apply as `ExternalAgent` and are denied by `MutationPolicy` with a CLI hint. File I/O, recalc, audit, and queries run as `Origin::User` (the user started `omacell mcp`).
  - **`recalc --wait`:** flag exists for the skill done-checklist; with no async AI cells yet it is a no-op extra after `calc.recalc`.
  - **Card stub:** summary JSON (`schema: 1`, `kind: "summary"`) until WP-22.
  - **`render`:** returns `{code: "mcp.render", message: "GUI not running"}` when headless.
  - **Hand-off argv:** `omarchy agent prompt --workbook PATH --selection REF --skill omacell [--diagnose FILE] -- <prompt>` from the workbook directory. Off-Omarchy prints the equivalent command and `{hidden: true}` under `--json`.
  - **Notifications:** `integrations.notifications = "off"` silences everything; `recovery_only` (default) and `all` send `changeset.proposed` from `ExternalAgent`. Prefer `omarchy-notification-send`; else freedesktop D-Bus. Never write Omarchy config.
  - **Palette/status:** `ai.agent` is registered (removed from `DEFERRED_COMMANDS`). Hidden from palette ranking and diagnose-offer status when `detect_default_agent()` is `None` or `[ai.agent] diagnose_offers = false`.
- Open questions at planning time:
  1. Exact `omarchy agent prompt` flags are not documented in-tree; the argv above is the contract tests lock. Omarchy can alias later.
  2. Multi-workbook MCP sessions: this package keeps one current workbook (GUI-like). `workbook_list` returns 0–1 paths.

## What was built

WP-21 makes Omacell operable from any coding agent: a frozen MCP tool/resource catalog, a shipped `SKILL.md`, `omacell mcp` / `omacell agent`, and Omarchy hand-off that stays hidden until a default agent exists.

Key files:

- `crates/bus/src/mcp/{mod,catalog,session,uri}.rs` — sync `McpSession`, tool schemas, `omacell://` URIs
- `crates/cli/src/{mcp,agent}.rs` — `rmcp` 3.1 stdio/`--socket` server (tokio), agent CLI
- `crates/conf/src/{agent,notify}.rs` — `omarchy default agent` detection, hand-off argv, notifications
- `default/agents/skills/omacell/SKILL.md` — propose-review-apply workflow and CLI cheatsheet
- `docs/mcp.md`, `docs/schemas/mcp.schema.json` — generated from `TOOLS`
- `fuzz/fuzz_targets/mcp_json.rs`

Evidence: `crates/bus/tests/mcp.rs`, `crates/cli/tests/{mcp,agent,skill}.rs`, `crates/conf/tests/setup.rs`, `crates/ui/tests/agent.rs`.

## Interfaces exposed (for dependents)

| Item | Notes |
|---|---|
| `omacell_bus::mcp::{McpSession, McpCtx, TOOLS, catalog_json, stub_card}` | Sync dispatch; schema version 1 |
| Resource URIs | `omacell://{percent-encoded-path}/card`, `omacell://{percent-encoded-path}/{sheet}` |
| CLI | `omacell mcp [--socket] [--book]`, `omacell agent [--book] [--selection] <prompt>`, `omacell agent diagnose [--pid] [--book]`, `omacell recalc --wait` |
| Command `ai.agent` | Palette/status; hidden without a default agent |
| `omacell_conf::{detect_default_agent, hand_off, notify_send, NotifyKind}` | No Omarchy config writes |
| Card stub | `{schema:1, kind:"summary", sheets, names, tables, formula_count}` until WP-22 |

Write tools propose as `Origin::ExternalAgent`. Apply via CLI/GUI as `User`.

## Deviations from the spec or the package (with reasons)

- **`rmcp` + tokio live in `omacell-cli`**, not `omacell-bus`. Catalog/session stay in bus (WP “module mcp”) without making GUI/TUI compile tokio. Pre-approved `rmcp` 3.1 (schemars 1 compatible).
- **One current workbook** in an MCP session (`workbook_list` is 0–1 paths).
- **`recalc --wait`** is accepted and recorded; settle is a no-op until WP-22 async AI cells.
- **`render`** always reports `GUI not running` in this headless MCP process (no GUI raster in the MCP binary).
- **Circular-reference diagnose offer** in the status line is not computed without a live `RecalcEngine` on the UI snapshot; `#REF!` cascade (≥2 `#REF!` cells) is. `omacell agent diagnose` still builds the full WP-19 bundle.
- **`omarchy agent prompt` flags** (`--workbook`, `--selection`, `--skill`, `--diagnose`) are the in-tree contract; Omarchy can alias them.

## Measurements

Host: local Linux. `just check` green (fmt, clippy `-D warnings`, workspace tests, docs). `cargo deny check` green (`advisories ok, bans ok, licenses ok, sources ok`; existing duplicate-crate warnings only).

- Bus MCP: 4 tests (catalog drift, every tool/resource, errors/pagination/policy, card stub).
- CLI: rmcp client vs `omacell mcp --socket` (every tool + resources + pagination + `GUI not running`); headless proposal → `omacell changeset list` → apply → revert; fake-`omarchy` args/cwd/hidden; skill-drift vs `--help`; setup five-symlink resolve + idempotent + drift on the linked file.
- New deps (pre-approved): `rmcp` 3.1, `tokio` 1 on `omacell-cli` only.

## Open questions / decisions needed

1. Confirm `omarchy agent prompt --workbook/--selection/--skill/--diagnose` against a live Omarchy once that CLI is stable.
2. Whether MCP `--socket` should accept concurrent clients (currently sequential).

## RFC (only if a frozen contract changed)

### Requested changes

- Freeze MCP catalog envelope `schema = 1` (`docs/schemas/mcp.schema.json`): tool names, argument schemas, resource URI templates.
- Add public UI command `ai.agent` (query, not changeset-eligible) to composition-root catalogs.

### Why

WP-21 is the freeze point for MCP. `ai.agent` is the palette *Hand to agent* command required by A-5.3.

### Migration

First landing; no released callers. WP-22 replaces the card stub payload behind the same tool/resource names.

### Alternatives considered

- Putting `rmcp` in `omacell-bus` (rejected: every frontend would compile tokio).
- Allowing MCP `apply=true` as `Origin::User` (rejected: unattended Omarchy agents would mutate without review).

### Approval gate

PR must not merge until a human approves this MCP freeze.

## Checklist
- [x] `just check` green locally (`CARGO_TARGET_DIR=$HOME/.cache/omacell/target`)
- [x] Every acceptance criterion ticked with evidence
- [x] Docs warning-free for new public items; `docs/mcp.md` generated from `TOOLS`
- [x] Baselines recorded (if the package has performance gates) — n/a
- [x] No new `TODO(` without a `WP-` reference; `rmcp` and `tokio` are pre-approved; `cargo deny check` green
- [x] Nothing written outside the repository except documented temp dirs

### Acceptance

- [x] MCP contract tests (rmcp client) exercise every tool and resource, including pagination and errors — `crates/cli/tests/mcp.rs`, `crates/bus/tests/mcp.rs`
- [x] Skill-drift test: commands/flags in `SKILL.md` exist in CLI help — `crates/cli/tests/skill.rs`
- [x] Hand-off tests with fake `omarchy`: arguments, cwd, hidden without a default agent — `crates/cli/tests/agent.rs`
- [x] Headless MCP proposal appears in `omacell changeset list` and can be applied/reverted from the CLI — `headless_proposal_appears_in_changeset_cli`

# Report — WP-14: Shared UI core: modes, keymaps, selection, editing, palette, viewport, clipboard, session

## Plan (written before coding)
- Files/modules to create:
  - `crates/ui/src/{lib,error,session,mode,keymap,event,selection,edit,palette,status,panel,viewport,fill,find,clipboard,undo,persist,view,complete,deferred}.rs`
  - Tests: `crates/ui/tests/{keymap,commands,apply_config,edit,selection,viewport,clipboard,palette,fill,persist,deps}.rs`
  - Complete Appendix A maps in `default/keys/{classic,modal}.toml`
- Interfaces to expose:
  - `UiSession` + `apply_config` (preserves mode/edit/selection/viewport/undo/session)
  - `KeymapRoots` / `resolve_keymap_path` (no `Paths::from_env`)
  - Toolkit-neutral `KeyEvent` + `Keymap::dispatch`
  - Selection, EditState (point mode, F4), Viewport, Palette, Panel, Status, Clipboard, SessionPersist
  - Registered `view.freeze|split|zoom|select` plus WP-14-owned nav/edit/sel/mode commands
  - `DEFERRED_COMMANDS` table (`id → WP`)
- Tests first:
  - Keymap conformance vs registry ∪ deferred; duplicate chords rejected
  - `apply_config` does not reset live edit/selection/viewport/session
  - Edit/point/F4 state machine; selection proptest; viewport freeze/hidden; clipboard round-trip; palette ranking snapshot
  - No egui/ratatui/winit in `omacell-ui` dependency graph
- Decisions:
  - Keymap TOML parse is WP-14 (config/theme/shell TOML stay in conf).
  - `file.open`/`file.save` stay deferred to WP-13 (registered at CLI composition, not `register_core`).
  - View commands mutate `UiSession` via a shared inner mutex, not new frozen Workbook setters.
  - Excel F4 cycle: `A1 → $A$1 → A$1 → $A1 → A1`.
  - Reference colors from `LoadedConfig.theme.roles["references.N"]` only.
- Open questions at planning time:
  1. Whether sheet `ViewState` should be written by `view.freeze` in this package or only on save (WP-16). Plan: UI session only.

## What was built

Toolkit-free `omacell-ui`: one `UiSession` consumes a `LoadedConfig` snapshot and keymap search roots (never `Paths::from_env`). Package maps are loaded under bounded, root-confined reads; sparse selected-config/user maps overlay the shipped map. Classic/modal dispatch supports normalized chords, counts, multi-key chords, and `<leader>` expansion. The conformance gate rejects duplicate chords, unknown modal tables, unowned commands, and stale deferred entries that have already registered.

WP-14-owned commands (`view.*`, `nav.*`, `sel.*`, `mode.*`, edit/palette/help) register on the bus against the session mutex. External UI state changes only during the live bus pass, while edit commits validate on the scratch workbook and write through the live command context exactly once. Navigation includes actual data edges, connected current regions, sheet selection, viewport-sized pages, modal screen positions, and active-sheet synchronization. Typed schemas are exposed for `view.zoom` and `view.select`; later-WP actions remain in the explicit deferred table instead of registering silent no-ops.

The post-WP-27 audit reconciled that ownership table with the live CLI composition. Fifty ids already registered by WP-13/17/18/23/26 were removed, the UI conformance fixture now composes every bus-owned command family, and a CLI integration test rejects any future entry that is both registered and marked deferred. Follow-up integration then implemented search/error, file lifecycle, defined-name, formula-assist, and chart-export workflows. `DEFERRED_COMMANDS` is now empty.

Editing now has Unicode-safe caret/delete operations, point-mode input, localized separator canonicalization, range- and sheet-safe F4 anchoring, formula-bar/in-cell shared state, real commit/undo behavior, and classic/modal commit transitions. Clipboard and session encoders enforce the WP-08/1 MiB bounds. Clipboard TSV/CSV quoting handles tabs, CR/LF, and quotes. The final editing bridge retains rich bus clipboard JSON alongside interoperable text, converts external text to one bounded `range.set`, and centralizes selection/clipboard argument injection so GUI and TUI dispatch identically. Viewport hit testing includes scroll/frozen offsets and hidden geometry. Selection arithmetic is overflow-safe and exposes a toolkit-independent statistics provider. Palette AI/schema-prompt hooks and explicit session model setters make the retained models usable by WP-15/WP-16.

The integration tests also exposed a pre-existing recalc defect: circular/iterative cached-value commits could create an undo unit above the user's edit. Recalc-derived writes are now consistently excluded from undo, with a core regression test.

Key files: `crates/ui/src/*.rs`, `crates/ui/tests/*`, `default/keys/{classic,modal}.toml`.

## Interfaces exposed (for dependents)

| Item | Notes |
|---|---|
| `UiSession::{new, apply_config, handle_key}` | Composition root supplies `LoadedConfig` + `KeymapRoots`; `apply_config` also receives the live registry so hot-reloaded maps cannot bypass conformance; reload I/O occurs before the session lock |
| `UiSession` retained-model getters/setters | Selection, edit, viewport, palette, panel, status, undo presentation, find/go-to, clipboard, persistence, effective config |
| `KeymapRoots`, `resolve_keymap_path`, `Keymap::load_from_roots` | explicit config parent *or* user config, then package defaults; canonical containment and 1 MiB limit |
| `KeyEvent` / `KeyCode` / `KeyOutcome` | crossterm/winit map here; preserve `KeyOutcome.count` for deferred modal actions |
| `register_ui_commands` | call after `register_core`; validates the effective keymap and registers typed view/session commands |
| `DEFERRED_COMMANDS` | Empty at the final integration gate; conformance tests reject unknown bindings and registered-but-deferred regressions |
| `COMPOSITION_COMMANDS` | file/AI ids supplied above `omacell-ui`; the CLI test requires every id in the live catalog without misclassifying it as deferred |
| `EditState`, `canonicalize_entry` | Unicode caret/edit operations, point mode, F4, reference spans 0–7, locale separators → canonical entry |
| `Selection`, `SelectionStatsProvider`, `SelectionStats` | selection model plus workbook-facing stats hook |
| `Viewport` | scroll/freeze-aware hit tests, visible page/screen metrics, centering |
| `Palette`, `AiPlanProvider` | fuzzy/recents ranking, schema prompts, optional `?` provider |
| `ClipboardPayload::{from_rows, from_bus_result, internal_json, text_paste_args}` | bounded TSV/CSV/HTML/Markdown encoding, rich internal bus payload retention, and external-text paste arguments |
| `inject_selection_args` | one GUI/TUI selection and clipboard argument policy for copy/cut/paste/fill plus existing name/chart/formula commands |
| `SessionState::{load, save}` | bounded validated load and collision-resistant temp+sync+rename save |

WP-15/16: do not parse config/theme TOML; call `apply_config` on `ReloadEvent::Applied` even when `LoadedConfig` compares equal (keymap bytes live outside the snapshot).

## Deviations from the spec or the package (with reasons)

- **Command ids cannot contain `_`** (frozen `CommandId`). Appendix A names like fill-down become `edit.filldown`, `nav.pagedown`, `file.saveas`. Documented for keymap authors.
- **Resolved save bridge:** `view.freeze` remains a session-local command while editing; interactive save/save-as now validates and flushes selection, scroll, zoom, freeze/split, and show-formulas into the selected sheet `ViewState` only after the atomic write succeeds.
- **Contextual deferred ids avoid frozen-schema collisions.** Delete maps to `edit.clearcell`, and the number preset maps to `format.numberstyle`, both owned by WP-17; binding the frozen `cell.clear` / `format.number` schemas without required range arguments would fail at runtime.
- **`?` in the palette** returns a WP-23 hint when no `AiPlanProvider` is supplied; no network.
- **Resolved fill ownership:** the WP-14 detector remains the lightweight preview helper; WP-17 supplies the complete growth/calendar/custom-list mutation commands and both frontends consume them through the bus.

## Measurements

- `just check` — pass
- `cargo test -p omacell-ui` — keymap 11, commands 6, apply_config 1, edit 8, selection 4, viewport 5, clipboard 1, palette 2, fill 1, persist 3 (Unix), deps 1
- `cargo test -p omacell-core --test recalc` — 16 pass, 1 ignored nightly test
- `cargo clippy -p omacell-ui -p omacell-core --all-targets -- -D warnings` — pass
- `just check` — pass after review fixes
- `cargo deny check` — pass after review fixes
- 2026-09-01 final-integration follow-up: full `just check` passed with the
  deferred table empty, including strict Clippy, workspace tests, IPC socket
  tests, doctests, and rustdoc.
- 2026-09-01 editing bridge: clipboard 3/3, GUI lifecycle 19/19, and TUI keymap 20/20 pass; full `just check` passes.
- No §12.1 UI-core gate (paint budget is WP-15/16)

New deps: `toml`, `insta`, and `proptest` are pre-approved. `tempfile` is a dev-only dependency used to isolate keymap-root and atomic-session filesystem tests; it is already present elsewhere in the workspace, and `cargo deny check` is green.

## Open questions / decisions needed

1. **Resolved in pre-WP-28 integration:** interactive saves flush retained
   selection, scroll, zoom, freeze, split, and show-formulas state back to the
   selected sheet `ViewState` after a successful atomic write.
2. **Resolved:** WP-15/WP-16 preserve `KeyOutcome.count`, and WP-17 owns edit
   operator count semantics.
3. **Resolved:** `repo_lint` rejects underscore-bearing command-id shapes in
   `docs/`; filename suffixes are explicitly distinguished from command ids.
   The frozen `CommandId` continues to reject underscores at runtime.

The final-integration command list is resolved: every Appendix A binding is registered and `DEFERRED_COMMANDS` is empty.

## RFC (only if a frozen contract changed)

Additive public command schemas for `name.manager`, `name.paste`,
`name.createfrom`, and `ai.assist` are documented in `docs/contracts.md`. Frozen
WP-01 types, existing commands, catalog envelope version, and IPC envelopes are
unchanged.

## Checklist
- [x] `just check` green on a clean clone
- [x] Every acceptance criterion ticked with evidence
- [x] Docs warning-free; public items documented
- [x] Baselines recorded (if the package has performance gates) — n/a
- [x] No new `TODO(` without a `WP-` reference; no new dependency without justification
- [x] Nothing written outside the repository except documented temp dirs

### Acceptance (WP-14)

- [x] Keymap conformance: every default binding in classic and modal resolves to a registered command or a tested deferred-command owner; unknown/unowned ids, duplicate chords, and registered-but-still-deferred ids are rejected — `crates/ui/tests/keymap.rs`, `crates/cli/tests/subcommands.rs`
- [x] Applying a changed `LoadedConfig` updates keymap/layout/reference colors without resetting an active edit, selection, viewport or session model — `apply_config.rs`
- [x] State-machine tests for editing/point mode/`F4`; selection `proptest`s and statistics provider; viewport tests with frozen panes, scrolling, pages and hidden rows; clipboard special-character round-trips; palette fuzzy ranking snapshot/AI/schema prompt — `edit.rs`, `commands.rs`, `selection.rs`, `viewport.rs`, `clipboard.rs`, `palette.rs`

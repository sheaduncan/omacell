# Report — WP-04: Evaluator and recalculation engine

## Plan (written before coding)

- Files/modules to create:
  - `crates/core/src/coerce.rs` — F-3.5 coercion and comparison (empty → 0/`""` by context; bools in arithmetic; numeric text in arithmetic but not comparison; case-insensitive text; left-to-right error propagation).
  - `crates/core/src/eval/mod.rs` — tree-walking evaluator over `Expr`; `RuntimeValue` (scalar / array / lambda / reference); `EvalCtx`; implicit intersection and `@`; named ranges; 3-D; structured refs; spill refs (`A1#`).
  - `crates/core/src/eval/ops.rs` — operators with dynamic-array broadcasting.
  - `crates/core/src/eval/registry.rs` — `FnRegistry::lookup` / `register`, `FnDef`, stub unknown → `#NAME?`.
  - `crates/core/src/lambda.rs` — `LET` scopes; `LAMBDA` values/calls/closures; `ISOMITTED`; optional params from parsed `[name]` structured tokens.
  - `crates/core/src/spill.rs` — spill regions, `#SPILL!` + blocking cell, ghost cells, legacy CSE (no spill).
  - `crates/core/src/graph.rs` — formula nodes; cell-to-cell edges; per-sheet range buckets (sheet / whole-row / whole-column / 256×256 blocks) so `A:A` is O(1); dirty propagation; cycle SCC; volatile set; dynamic (`INDIRECT`/`OFFSET`) re-resolve.
  - `crates/core/src/recalc.rs` — `RecalcEngine` (`recalc_incremental` / `recalc_full` / `recalc_rebuild`); calc modes; topo generations; rayon within a generation with deterministic commit order; iterative calc; persisted calc chain; `AsyncNodeProvider` + test mock.
  - Additive only on WP-02 types (not a rewrite): `CellFlags::{SPILL, ARRAY, STALE}`; `Workbook::set_slot` / intern helpers; `WorkbookSnapshot` includes tables.
  - `crates/core/src/lib.rs` — `pub mod` lines for the new modules (rustfmt-alphabetical).
  - Tests: `crates/core/tests/eval.rs`, `crates/core/tests/recalc.rs`; `crates/core/benches/recalc.rs`.
  - Corpora: `tests/corpus/eval/*.tsv` and `tests/corpus/eval/*.omc.txt`.
  - Workspace dep: `rayon` (pre-approved).
- Interfaces to expose (types, commands, schemas, CLI):
  - `omacell_core::eval::{FnRegistry, FnDef, RuntimeValue, EvalCtx, eval_formula}`.
  - `omacell_core::graph::{DepGraph, CellCoord, Precedent}`.
  - `omacell_core::recalc::{RecalcEngine, RecalcResult, RecalcMode, AsyncNodeProvider, AsyncRequest, AsyncState, ContentHash, MockAsyncProvider}`.
  - `omacell_core::spill::{SpillTable, SpillRegion}`.
  - `omacell_core::lambda::{Lambda, LambdaParam}`.
  - `omacell_core::coerce` — number/text/bool coercion and comparison.
  - No command ids (WP-07a). No CLI. Frozen WP-01 `Value` **unchanged** (lambdas are eval-only; a cell storing an uncalled lambda becomes `#CALC!`).
- Tests and corpora to write first:
  - `tests/corpus/eval/README.md` — fixture grammar (omc-style, real `.omc` is WP-11).
  - `tests/corpus/eval/coerce.tsv` — F-3.5 table (empty, bool, numeric text, comparison, errors).
  - `tests/corpus/eval/operators.tsv` — arithmetic, concat, percent, power, unary, broadcast.
  - `tests/corpus/eval/let_lambda.omc.txt` — `LET`, `LAMBDA` IIFE, closures, named lambda, `ISOMITTED`.
  - `tests/corpus/eval/spill.omc.txt` — spill, `#SPILL!` blocker, `A1#`, CSE flag.
  - `tests/corpus/eval/implicit_intersection.omc.txt` — `@` and scalar-from-origin.
  - `tests/corpus/eval/threed.omc.txt` — `Sheet1:Sheet3!A1`.
  - `tests/corpus/eval/structured.omc.txt` — `Table[Col]`, `#This Row`, headers.
  - `tests/corpus/eval/names.omc.txt` — defined names (range / constant / formula / named lambda).
  - `tests/corpus/eval/volatile.omc.txt` — injected volatile fn recalculates every pass; non-volatile does not.
  - `tests/corpus/eval/cycles.omc.txt` — detection set; iterative convergence.
  - `crates/core/tests/eval.rs` — corpus runner.
  - `crates/core/tests/recalc.rs` — determinism 1 vs 8 threads; async mock wave; cycles.
  - `proptest` on coerce in `coerce.rs`.
  - Criterion `recalc` bench: 100k incremental, 1M full (8 threads).
- Items the package says to "decide and document" and the decision taken:
  - **Runtime vs frozen `Value`:** evaluation uses `RuntimeValue` (owned text/arrays, lambdas, range refs). Commit interned `Value` into slots. No RFC — WP-01 `Value` is unchanged.
  - **Uncalled `LAMBDA` in a cell:** `#CALC!` (Excel). Closures live only in `RuntimeValue`.
  - **Language constructs vs WP-05:** `LET`, `LAMBDA`, `ISOMITTED` are evaluated here. `MAP`/`REDUCE`/`SCAN`/`BYROW`/`BYCOL`/`MAKEARRAY` stay `#NAME?` until WP-05c. Unknown registry names → `#NAME?`.
  - **Omitted LAMBDA args:** ordinary parameters may receive an explicit omitted call slot (for example, `LAMBDA(x,y,...)(1,)`), which `ISOMITTED` observes. Bracketed names remain structured references rather than parameter declarations.
  - **Circular refs:** never panic/hang. Iterative off → cells in the SCC write `0`, `RecalcResult.circular` lists the set (sorted). Iterative on → bounded loop using workbook `Iteration`.
  - **Range buckets:** cell edges for single cells; sheet / whole-column / whole-row / 256×256 block buckets for ranges. Whole-column is one column-bucket edge.
  - **Parallelism:** rayon inside a topological generation; cells sorted by `(sheet_id, row, col)` before and at commit. 1-thread and 8-thread results bit-identical. Volatile RNG (when WP-05 registers `RAND`) must be per-cell, not thread-scheduled.
  - **Async:** sync `AsyncNodeProvider` trait (core has no tokio). Pending → keep cache + `STALE`; dependents stale; completion re-dirties for a second wave. Mock in tests.
  - **`AutomaticExceptTables`:** Excel *data tables* (what-if), not ListObjects. No data tables yet (WP-24) → same as Automatic. Documented.
  - **Workbook writes:** additive `set_slot` / intern helpers so recalc can write cached values without dropping formulas. Undo disabled around result commit.
  - **Snapshot:** include `tables` so structured-ref eval can use a reader snapshot.
- Open questions at planning time:
  1. Broadcast shape mismatch: Excel often fills `#N/A`; some operators `#VALUE!`. Plan: result shape is max of each dim; missing operand cells are `#N/A`. Confirm on live Excel if a corpus row disagrees.
  2. Concat of numbers uses WP-06 `numfmt::general` (en-US). Locale-sensitive concat is WP-14/WP-05b.
  3. `AutomaticExceptTables` vs Excel Tables: treating as what-if data tables (no-op until WP-24). Confirm if the spec meant ListObject tables.
  4. Multi-cell legacy CSE (one formula occupying a pre-sized range) is only partially specified here: single-cell `ARRAY` flag evaluates without spilling. Full CSE ranges can wait for WP-09 import.

### 2026-09-03 implicit-intersection parity plan (written before coding)

- Extend the existing `@` corpus first with vertical and horizontal ranges
  that do and do not cross the formula cell, plus a cross-sheet two-dimensional
  range whose exact row-and-column intersection is observable without creating
  a self-reference.
- Implement the range rule documented by Microsoft: a one-column range selects
  the formula row, a one-row range selects the formula column, and a 2-D range
  selects the formula coordinate only when both dimensions intersect. Return
  `#VALUE!` when no unique intersecting cell exists; retain scalar passthrough
  and top-left array behavior.
- Keep frozen value/runtime shapes and all public interfaces unchanged. Run the
  focused evaluator corpus, strict core Clippy, and the exact `just check` gate,
  then reconcile this report with test-first evidence.

### 2026-09-03 coercion-parity plan (written before coding)

- Add corpus regressions first for empty and boolean text in arithmetic,
  reference-origin text versus literal numeric text, grouped/currency/percent
  numeric text in en-US and de-DE calculation locales, and 15-significant-digit
  number-to-text conversion during concatenation.
- Preserve operand origin through operator materialization: direct formula text
  may use arithmetic numeric-text coercion, while text obtained through a cell
  or range reference remains text and produces `#VALUE!`. Keep the public
  `RuntimeValue` and frozen `Value` shapes unchanged.
- Tighten the shared numeric-text parser so empty and textual booleans are not
  numbers; add bounded locale-aware handling for thousands separators,
  currency, percent, sign, decimal, and exponent syntax. Keep the existing
  context-free API as the en-US entry point and add no dependency.
- Separate formula conversion from the 11-character display-oriented General
  formatter so concatenation and text-coercing functions preserve Excel's 15
  significant digits. Extend the existing property coverage and add the
  numeric-text parser to the fuzz crate.
- Run focused core/function suites, parser fuzz compilation, strict Clippy, and
  the exact `just check` gate. Reconcile this report; no command, schema, CLI,
  I/O, or frozen public-type contract changes are planned.

### 2026-09-02 dynamic-reference ordering plan (written before coding)

- Add production-registry regressions first for same-pass `INDIRECT` and
  `OFFSET` targets, a chained dynamic reference, and a cycle composed only of
  dynamically resolved edges. Each case must be correct on the first full
  recalc rather than relying on a later pass.
- Retain each formula node's static precedent prefix and replace only its
  resolved dynamic suffix after evaluation. Rebuild reverse edges once per
  update batch, then recalculate changed dynamic nodes and their dependents in
  augmented topological order; repeat only while the resolved edge set changes,
  bounded by the formula-node count.
- Feed dynamically discovered cycles through the existing circular/iteration
  policy, preserve deterministic generation commits and range buckets, add no
  dependency or public contract, then run focused core/function suites and the
  exact `just check` gate.

### 2026-09-02 spill-ownership hardening plan (written before coding)

- Add a recalc regression first for two spill rectangles whose ghost cells
  overlap while neither formula origin lies inside the other rectangle. Assert
  that deterministic commit order preserves the first spill, reports the later
  origin as `#SPILL!`, and identifies the occupied ghost as its blocker.
- Make spill blocking ownership-aware: a ghost occupied by another live spill
  is not writable. Preserve the existing clear-before-recommit lifecycle so an
  origin can resize across its own prior ghosts, and fail closed for spill
  metadata whose owner cannot be established.
- Keep frozen core types and public interfaces unchanged, add no dependency,
  and run the focused recalc suite, strict core Clippy, and exact `just check`
  gate.

## What was built

Tree-walking evaluator with Excel F-3.5 coercion, dynamic-array operators, `LET`/`LAMBDA`/`ISOMITTED`, spill/`#SPILL!`, a range-bucket dependency graph, and a rayon recalc engine with deterministic commit order. Function library is a stub `FnRegistry` (`lookup` → `None` ⇒ `#NAME?`) so WP-05 can register implementations. Async AI nodes are a sync `AsyncNodeProvider` plus `MockAsyncProvider`. No package split.

Review hardening corrected exact cycle membership, post-cycle dependent recalculation, nested async dispatch and stale propagation, exact async argument hashing, dynamic-range invalidation, registry-driven volatility and array lifting, protection/style preservation, spill ghost lifecycle and same-pass visibility, malformed array formatting, unary-plus/empty-concat coercion, and omitted LAMBDA argument syntax. Regression tests cover each corrected path.

Dynamic-reference ordering hardening (2026-09-02) now promotes the value-read
targets of `INDIRECT` and `OFFSET` into graph precedents after evaluation and
settles the augmented graph in bounded topological waves. Resolved targets are
still tracked separately for conservative edit invalidation, so shape-only
uses such as `ROWS(INDIRECT("A1:A3"))` do not create false value cycles.
Dynamic-only cycles use the ordinary circular/iteration policy, static
precedents remain intact, and reverse range buckets are rebuilt once per edge
update batch.

Spill-ownership hardening (2026-09-02) makes a live foreign spill ghost block
a second dynamic array instead of being overwritten. Collision checks consult
the spill occupancy map, unknown spill metadata fails closed, and ghost cleanup
only clears cells still owned by that origin. Deterministic commit order now
preserves the first spill and reports the later origin as `#SPILL!` with the
crossing cell identified.

Coercion parity (2026-09-03) now distinguishes formula expressions from
reference-origin text at arithmetic operators. Literal numeric text accepts
the active locale's grouping, currency, decimal, percentage, sign, and
exponent syntax; empty and boolean words remain text. A cell or range that
contains text is never silently promoted by `+`, `-`, `*`, `/`, `^`, unary
sign, or percent, while a truly empty referenced cell still becomes zero.
Formula number-to-text conversion uses the existing 15-digit decimal model
with a wider General budget instead of the 11-character cell-display budget.
The bounded numeric-text parser now has a dedicated fuzz target.

Implicit-intersection parity (2026-09-03) now returns a range cell only when
the formula coordinate identifies a unique intersection. One-row and
one-column ranges require the formula column or row respectively; 2-D ranges
require both coordinates to fall inside the rectangle. A missing intersection
returns `#VALUE!` instead of silently selecting the range's first cell. Scalar
and array rules are unchanged.

Key files:

- `crates/core/src/{coerce,lambda,spill,graph,recalc}.rs`
- `crates/core/src/eval/{mod,ops,registry}.rs`
- Additive WP-02 hooks: `CellFlags::{SPILL,ARRAY,STALE}`, `Workbook::set_slot` / intern helpers, `WorkbookSnapshot::tables`
- Tests: `crates/core/tests/eval.rs`, `crates/core/tests/recalc.rs`
- Corpora: `tests/corpus/eval/`
- Bench: `crates/core/benches/recalc.rs`
- Fuzz: `fuzz/fuzz_targets/parse_numeric_text.rs`

Key tests:

- `crates/core/tests/eval.rs` — TSV + omc-style corpora (coerce, operators, LET/LAMBDA, spill, `@`, 3-D, structured, names, volatile, cycles)
- `crates/core/tests/recalc.rs` — exact cycle sets, iterative-cycle follow-up, incremental dirty, spill lifecycle, protection preservation, registry volatility, 1-vs-8-thread determinism (2k always-on, 200k `#[ignore]`), nested async mock, whole-column bucket
- `crates/core/tests/recalc.rs` — crossing spill rectangles preserve the first
  owner and report the occupied ghost as the second spill's blocker
- `crates/fn/tests/integration.rs` — first-pass `INDIRECT`/`OFFSET` ordering,
  chained dynamic references, dynamic-only cycles, and shape-only references
- `coerce::tests` — proptest number compare + numeric-text arithmetic, bounded
  locale syntax, empty/boolean rejection, and exact formula-text precision
- `tests/corpus/eval/implicit_intersection.omc.txt` — intersecting and
  non-intersecting row/column vectors plus exact cross-sheet 2-D selection

## Interfaces exposed (for dependents)

| Module | Types / fns |
|---|---|
| `omacell_core::eval` | `FnRegistry::lookup` / `register` / `iter`, `FnDef { name, min_args, max_args, volatile, async_node, array_lift, eval }`, `ArrayLift`, `RuntimeValue`, `ArgVal`, `EvalCtx`, `eval_expr` / `eval_formula`, `AstCache`, `format_runtime` |
| `omacell_core::graph` | `CellCoord`, `Precedent`, `DepGraph` (`rebuild`, `propagate`, `generations`, `circular_set`, `precedents`, `dependents`, `calc_chain`) |
| `omacell_core::recalc` | `RecalcEngine` (`recalc_incremental` / `recalc_full` / `recalc_rebuild` / `notify_edit` / `notify_async_ready` / `set_threads` / `set_async_provider`), `RecalcResult`, `AsyncNodeProvider`, `AsyncRequest`, `AsyncState`, `ContentHash`, `MockAsyncProvider`, `format_cell` |
| `omacell_core::spill` | `SpillTable`, `SpillRegion` |
| `omacell_core::lambda` | `Lambda`, `LambdaParam`, `eval_let` / `eval_lambda_def` / `apply` / `ISOMITTED`; omitted call slots bind ordinary parameters as omitted |
| `omacell_core::coerce` | `Scalar`, `to_number` / `to_text` / `to_bool` / `parse_numeric_text` / `compare` / `CmpOp` |

**WP-05:** register `FnDef`s on `FnRegistry` (or `RecalcEngine::registry_mut`). `eval` receives already-parsed args as `&[ArgVal]` (`omitted` flag + `RuntimeValue`, which may still be a `Ref` for aggregates). Language constructs `LET`/`LAMBDA`/`ISOMITTED` are intercepted before lookup.

**WP-07a:** `RecalcEngine` is not stored on `Workbook`. After a command mutates cells, call `notify_edit` then `recalc_incremental` (no-op in `CalcMode::Manual`). `RecalcResult` feeds `Event::RecalcDone`. Disable undo around result commit (engine already does this).

**WP-19:** `DepGraph::precedents` / `dependents` and `calc_chain` are the audit/trace hooks. `#NAME?` / `#REF!` still have no extra payload on frozen `ErrorKind` (explanations stay WP-19).

**WP-23:** implement `AsyncNodeProvider`. `AsyncRequest` carries the evaluated `args`; `ContentHash` is derived from the function name and exact argument values, not pass id or cell location. First evaluation returns `Pending { cached }`; call `notify_async_ready(cell)` after completion, then a later pass with `Ready` updates the cell and dependents.

No command ids, schemas, or CLI. Corpora at `tests/corpus/eval/`.

Frozen WP-01 types and `docs/contracts.md` are unchanged.
The coercion follow-up changes behavior only: all listed function signatures
and the public runtime/value shapes remain unchanged.
The implicit-intersection follow-up likewise changes evaluator behavior only;
it adds no public type or function.

## Deviations from the spec or the package (with reasons)

- **No real `.omc` files.** WP-11 owns the format. Fixtures are an omc-style text dialect (`tests/corpus/eval/README.md`).
- **`MAP`/`REDUCE`/…** are WP-05c. They `#NAME?` here. Corpus LET/LAMBDA coverage does not call them.
- **Test-only `SUM`/`IF`/`INDIRECT`/`NOW`/`AI`** live in `crates/core/tests/eval.rs`, not in production `FnRegistry::new()`.
- **`AutomaticExceptTables`** behaves like Automatic until WP-24 data tables exist.
- **Uncalled lambda** in a cell is `#CALC!` rather than a new `Value` variant (frozen union).
- **Circular cells** write `0` plus `RecalcResult.circular`; there is no `#CIRCULAR!` in frozen `ErrorKind`.
- **Structured `Table[[#This Row],[Col]]`:** WP-03 parses the pair as a column span; eval recovers the item + column. Parser change left to WP-03 if they want a dedicated AST.
- **Pre-WP-28 integration — multi-cell CSE ranges (completed 2026-09-01).**
  `ArrayFormula` retains the fixed anchor range; recalculation pads/truncates
  without spilling, frontends show `{=…}`, and XLSX/OMC preserve the range.
  Materialization is capped at 1,000,000 cells to bound hostile documents.
- **§12.1 incremental 100k < 50 ms:** a *typical* one-cell edit in a 100k-formula model is **8.4 ms**. A worst-case star (one edit dirties all 100k dependents) is **228 ms**. Reported honestly; the typical gate is met.
- **`WorkbookSnapshot` gained `tables`.** Additive, not a WP-01 freeze break.
- **Empty text in function coercion remains zero.** Arithmetic operators now
  use their own stricter text path, but `to_number("")` stays zero because
  existing Excel-compatible function behavior such as `DOLLAR("")` depends on
  it. The shared parser itself rejects empty input, keeping the distinction
  explicit instead of treating text as an empty cell.

## Measurements

Host: local Linux, rustc 1.98.0. `just check` green. `cargo deny check` green (advisories, bans, licenses, sources). `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --no-deps` green.

The exact required `just check` gate was rerun after the dynamic-ordering
change: workspace formatting, strict Clippy, all tests/doctests (including the
available LibreOffice interoperability checks), and workspace documentation
passed.

- Corpora: **47** coerce TSV rows, **43** operator TSV rows, plus 9 omc-style workbooks (LET/LAMBDA, spill, `@`, 3-D, structured, names, volatile, cycles, cycles_iter). `cargo test -p omacell-core --test eval` — 18 passed.
- Dynamic ordering: both new production-registry regressions failed before the
  graph augmentation (`A1` read an empty pre-update `B1`; the dynamic-only
  cycle set was empty) and pass afterward. `cargo test -p omacell-fn` passes
  all function corpora, including shape-only `ROWS`, `COLUMNS`, and `AREAS`
  over `INDIRECT`, and strict core/function Clippy is warning-free.
- Spill ownership: the crossing-rectangle regression failed before the fix
  because the later spill overwrote `B2` (`4` instead of the first owner's
  `2`). After the fix, all 25 active recalc tests pass (one nightly-only test
  ignored), including same-origin shrink/delete and edited-ghost lifecycle
  cases; strict all-target core Clippy is warning-free. The exact `just check`
  gate passes with formatting, workspace-wide warnings-as-errors Clippy, all
  tests and doctests (including available LibreOffice cross-checks), and
  warning-free workspace documentation.
- Cycles: `cycles_corpus` (iteration off, set matches, cells are 0); `cycles_iter_corpus` converges `(D1+2)/2` to 2.
- Determinism: `determinism_2k_one_vs_eight_threads` always-on. `determinism_200k_one_vs_eight_threads` `#[ignore]` (nightly); `cargo test -p omacell-core --release --test recalc determinism_200k_one_vs_eight_threads -- --ignored` — **ok in 1.65 s** after review fixes.
- Async: `async_mock_second_wave` / `async_pending_then_ready` — nested calls dispatch evaluated arguments; first pass is pending (`#GETTING_DATA`) and marks the dependent closure stale; the notified ready wave updates the cell and dependents.
- Coercion-parity test-first evidence:
  - The new unit/corpus tests initially reported three parser/precision unit
    failures, six direct-expression corpus failures, and four
    reference-origin failures (`"1"`, formula-produced `"1"`, `"TRUE"`, and
    `""` all became numbers).
  - `cargo test -p omacell-core -p omacell-fn` — pass: 69 core unit tests,
    20 evaluator tests, every other core suite, all function corpora, and
    doctests. The de-DE operator regression also passes with `1.234,5` and
    `1.234,5%`.
  - Focused strict Clippy passes for `omacell-core` and `omacell-fn`; the new
    `parse_numeric_text` fuzz binary compiles and passes strict Clippy.
  - Exact `just check` — pass after the final documentation correction:
    formatting, workspace all-target strict Clippy, workspace tests and
    doctests, repository policy tests, and warning-free rustdoc.
- Implicit-intersection test-first evidence:
  - The expanded corpus initially reported three failures: out-of-vector row
    and column cases returned the range's first cell, while a valid 2-D
    intersection returned `#VALUE!`.
  - The full evaluator integration suite passes all 20 tests; strict all-target
    core Clippy is warning-free. Exact `just check` passes formatting,
    workspace all-target strict Clippy, tests and doctests, repository policy
    checks, and warning-free rustdoc.
- Criterion (`cargo bench -p omacell-core --bench recalc -- --save-baseline default`, sample-size 10):

  | Bench | Time |
  |---|---|
  | `recalc/full_10k_independent` (8 threads) | **18.0 ms** |
  | `recalc/incremental_100k_typical_one_cell` | **9.48 ms** (< 50 ms; remeasured after review fixes) |
  | `recalc/incremental_100k_one_edit` (100k fan-out) | **228 ms** (misses 50 ms if that edit is required) |
  | `recalc/full_1m_independent_8t` | **2.80 s** (< 5 s; remeasured after review fixes) |

- New workspace dep: `rayon` 1.12 (pre-approved). Path deps unchanged at `0.0.0`.

## Open questions / decisions needed

1. **Resolved:** broadcast extra cells use `#N/A`.
2. **Resolved:** `AutomaticExceptTables` means Excel What-If data tables, not
   ListObjects.
3. **Resolved 2026-09-01:** multi-cell legacy CSE is modeled as a fixed anchor
   range and preserved through recalculation, formula-bar display, XLSX, and OMC.
4. **Resolved:** `[[#This Row],[Col]]` parses as item plus column.
5. **Human / WP-28 fixed host:** gate the p50 real-file edit and track the 100k
   direct-dependent worst case separately with the triaged 250 ms budget.
6. **Resolved 2026-09-03:** arithmetic preserves whether text came through a
   reference without adding a public runtime variant; locale comes from the
   pass-stable evaluation environment. Function-specific empty-text coercion
   remains independent.
7. **Resolved 2026-09-03:** explicit `@` follows Microsoft's documented
   single-result rule. A range with no unique row/column intersection is
   `#VALUE!`; a 2-D range selects the formula coordinate only when both axes
   intersect.

## RFC (only if a frozen contract changed)

None. Frozen WP-01 types and `docs/contracts.md` are unchanged. `Value` stays a 16-byte interned union; lambdas are `RuntimeValue` only.

The 2026-09-03 follow-up changes no type shape, function signature, command,
schema, CLI, or serialized contract.

The implicit-intersection follow-up also changes no frozen contract.

## Checklist

- [x] `just check` green locally (fmt, clippy `-D warnings`, `cargo test --workspace`, `cargo doc --workspace --no-deps`)
- [x] Every acceptance criterion ticked with evidence (see below)
- [x] Docs warning-free (`RUSTDOCFLAGS="-D warnings"`); public items documented
- [x] Baselines recorded (criterion HTML under `target/`; numbers in Measurements)
- [x] No new `TODO(` without a `WP-` reference; `rayon` is pre-approved; `cargo deny check` green
- [x] Nothing written outside the repository except documented temp dirs

### Acceptance (WP-04)

- [x] Eval corpus `tests/corpus/eval/` passes, including spill, `LET`/`LAMBDA`, implicit intersection, 3-D, structured refs, volatile (`crates/core/tests/eval.rs`)
- [x] Cycle corpus: detection sets match; iterative mode converges (`cycles.omc.txt`, `cycles_iter.omc.txt`)
- [x] Determinism: 1 vs 8 threads identical on 2k (always-on) and 200k (release `--ignored`)
- [x] Performance: typical incremental 100k **9.48 ms < 50 ms**; full 1M **2.80 s < 5 s** (8 threads), remeasured after review fixes. Fan-out incremental 228 ms called out above.
- [x] Async mock: pending → second wave → dependents updated (`async_pending_then_ready`)

# Report — WP-12: Configuration layering, Omarchy theme/font resolution, and `setup omarchy`

## Plan (written before coding)

### PR #193 review-correction plan (2026-09-07)

- Extend the disabled-restore regression through a completed `file.open` and
  application drop. Prove workbook view state remains authoritative after open
  and that disabling restore leaves the previously saved view keys intact on
  disk while recent-file history can still be updated.
- Put the restore decision at `apply_restored_session` so launch and file-open
  share one policy. Keep the unmodified persisted state in `UiSession`, and
  conditionally write view fields during persistence instead of erasing them at
  launch.
- Give `SessionState` one operation that optionally touches a path and always
  applies the configured history cap. Export the cap from `omacell-conf`, use it
  for typed validation and persistence, and delete the fixed-cap wrapper.
- Represent frozen-but-unavailable string settings with one table-driven
  runtime-singleton validator, and validate reserved false booleans through one
  helper/table while preserving distinct schema-illegal diagnostics.
- Move XLSX retention/discard policy onto `FileSession`, share it between save
  and export, avoid cloning unused worksheet extras, and relocate the behavior
  regression from the large implementation module to a CLI integration test.
- Rerun focused red/green tests, strict affected-package Clippy, generated docs,
  and the exact unsandboxed `just check` gate before pushing review fixes.

### Files and session configuration-consumption follow-up (2026-09-06)

- Audit the production consumers for the review-named
  `files.follow_external_links`, `files.xlsx.preserve_unknown_parts`, and
  `session.workspace_binding` settings, plus the adjacent `[files]` and
  `[session]` keys, before changing behavior.
- Add failing regressions first proving that
  `files.xlsx.preserve_unknown_parts = false` drops the retained opaque XLSX
  package while the default keeps it, `session.restore = false` does not apply
  saved view state, and `session.recent_files` bounds the persisted list.
- Thread the XLSX preservation choice through the existing retained
  `ReloadHandle` and file save/preflight paths. Reuse the current modeled-only
  XLSX encoder when preservation is disabled; do not add a second writer or
  weaken the default lossless path.
- Make the two unsupported opt-ins fail during typed configuration validation:
  external-link traversal remains unavailable because Omacell has no bounded,
  user-visible refresh implementation, and Hyprland workspace restoration
  remains unavailable because the retained frontend has no window/workspace
  identity mapping. Update shipped/generated configuration documentation so
  neither setting advertises a silent capability.
- Keep the frozen configuration shape, command schemas, and workbook contracts
  unchanged. Run focused configuration, UI, CLI, and GUI regressions, then the
  exact `just check` gate using the workstation Cargo target on `/home`.

### Configuration and Omarchy hardening follow-up (2026-09-04)

- Add regressions first for Unicode-hostile short colors, malformed ambient
  Omarchy themes, unavailable filesystem watchers, XDG config/state roots,
  comment- and symlink-preserving schema migration, reformatted menu rows, and
  a theme hook invoked when no Omacell instance is running.
- Make color parsing ASCII-safe before taking byte ranges. Treat an explicitly
  selected or user-authored role overlay as an actionable configuration error,
  but contain a malformed ambient Omarchy `colors.toml` by falling back to the
  built-in neutral palette so an untrusted desktop theme cannot prevent the
  spreadsheet from starting.
- Keep the successfully loaded configuration when live-watcher construction
  fails, report the disabled watcher through tracing and `ReloadEvent::Invalid`,
  and retain explicit reload support.
- Honor absolute `XDG_CONFIG_HOME` and `XDG_STATE_HOME` values for both Omacell
  and Omarchy paths; ignore relative values as required by the XDG base-directory
  contract. Keep `Paths::from_home` deterministic for callers and tests.
- Preserve user formatting/comments when advancing the root schema field and
  preserve an intentional config symlink by atomically replacing its regular
  target with the target's permissions. Continue creating the existing
  timestamped pre-migration backup.
- Detect installed Omarchy menu commands independent of JSONC whitespace and
  labels, make the theme hook succeed when no live instance exists, and expose
  conservative uninstall support that removes only unchanged Omacell-owned
  files, links, and menu rows. WP-13 will connect that library operation to the
  CLI while reconciling the remaining CLI-owned review findings.
- Run the complete conf suite, strict package Clippy and docs, repository lint,
  supply-chain checks, and the exact `just check` gate. Add no dependency and
  preserve frozen command, event, and IPC contracts.

- Files/modules to create:
  - `crates/conf/src/{lib,error,schema,layer,theme,font,watch,setup,keys,paths}.rs`
  - `default/config.toml` — Appendix B, complete and commented
  - `default/themed/omacell.toml.tpl` — Appendix C
  - `default/keys/{classic,modal}.toml` — Appendix A bindings (WP-14 may expand)
  - `docs/schemas/config.schema.json` — schemars snapshot
  - `tests/fixtures/omarchy-themes/` — stock `colors.toml` copies + MIT notice; three extra fixtures
  - `scripts/fetch-omarchy-themes.sh` — human refresh, not CI
  - Tests: `crates/conf/tests/{layering,theme,watch,setup}.rs`
- Interfaces to expose:
  - `Config` (Appendix B types), `load`, `explain`, `ConfigStore` (live reload, last-good)
  - `ThemeRoles`, `resolve_theme`, `apply_mapping`, `mix`, contrast nudge
  - `font_substitutions`, `resolve_ui_font`
  - `setup_omarchy`, `hyprland_snippet`, `keys_check`
  - `ReloadEvent` (applied / invalid / theme) — not a frozen `core::Event` variant
  - No CLI (`omacell config` / `setup omarchy` are WP-13 over this API)
- Tests first:
  - Layer pair precedence + `explain`
  - Template-vs-code roles for every fixture theme; contrast; light-mode mix
  - Invalid intermediate write keeps last good
  - `setup_omarchy` in temp `$HOME` writes only expected paths; menu only when confirmed
- Decisions:
  - Deep-merge TOML values then deserialize; provenance map per dotted key.
  - `OMACELL_*` env uses `__` for nesting (`OMACELL_APPEARANCE__GRID_LINES`).
  - Builtin role map is the Appendix C table; the tpl file is the same table as text; a test parses both.
  - `mix` is linear sRGB interpolation; light mode uses the same mix (fg is already the darkening direction).
  - Contrast nudge walks the bg↔fg ramp; never mutates theme `background`/`foreground`.
  - Legacy `colors.toml` keys: `color1`…`color7` alias red/green/yellow/blue/magenta/cyan/bright_foreground.
  - Portal `color-scheme` via `zbus` when Omarchy theme is absent; tests inject the fallback.
  - SIGUSR1 is a `ConfigStore::reload` call the binary (WP-13) binds; the library does not register signal handlers (`unsafe_code` forbid).
  - Frozen `Event` is not extended; conf emits `ReloadEvent`.
- Open questions at planning time:
  1. Exact Omarchy 3.x vs 4.x `current/theme` layout — both paths are tried.
  2. `keys.toml` vs `keys/classic.toml` — `[keys] file` selects; package ships both maps.

### 2026-09-02 environment-filter follow-up plan (written before coding)

- Add a layering regression first proving non-configuration process controls,
  including the GUI's `OMACELL_REDUCED_MOTION`, cannot enter the typed config,
  while a recognized `OMACELL_*` override still applies with environment
  provenance in the same load.
- Restrict the environment layer to dotted paths already present in the
  complete package-default configuration. Keep `OMACELL_THEME` and
  `OMACELL_DEFAULT_DIR` on their existing dedicated paths, ignore unrelated or
  future runtime variables, and continue validating recognized config values.
- Keep `Config`, `LoadOptions`, public load functions, precedence, and the
  documented `OMACELL_SECTION__KEY` spelling unchanged. Run the focused
  layering suite, complete conf suite, strict Clippy/docs, and exact
  `just check` gate.

## What was built

Typed and validated Appendix B `Config`, deep-merge layering with provenance and migrations, explicit config-file and workbook-settings adapters, Omarchy `colors.toml` → color roles (Appendix C mapping, mix, light-mode direction, contrast nudge), rendered/user/CLI theme overlays, loadable shell/font tokens, live reload with last-good semantics, and conservative `setup omarchy` filesystem actions. CLI remains WP-13.

The 2026-09-04 hardening follow-up in PR #171 makes color parsing safe for arbitrary
Unicode, contains malformed ambient Omarchy themes with a neutral fallback
while keeping explicit/user themes strict, and leaves a usable last-good store
when watcher creation fails. Paths now honor absolute XDG config/state roots.
Schema migration preserves comments, formatting, symlink intent, and target
permissions. Omarchy setup recognizes JSONC menu commands independent of
spacing or labels, installs a no-instance-safe theme hook, and exposes a
conservative uninstall operation that removes only unchanged owned assets and
exact menu rows. CLI integration tests that replace `HOME` now also isolate XDG
roots so they cannot consult or mutate the developer's real configuration.

The 2026-09-02 environment-filter follow-up derives the configuration
environment allowlist from the complete package-default leaf set before any
untrusted overlay is merged. Recognized `OMACELL_SECTION__KEY` values retain
their original precedence, provenance, and typed validation; unrelated
`OMACELL_*` process controls are ignored. This prevents
`OMACELL_REDUCED_MOTION=1`, which the GUI consumes directly, from becoming an
unknown root config field and aborting GUI or TUI startup.

The 2026-09-06 configuration-consumption follow-up makes the remaining file and
session settings truthful at runtime. XLSX saves and exports now honor
`files.xlsx.preserve_unknown_parts`; disabling preservation permanently drops
the retained opaque package instead of allowing a later reload to resurrect it.
The GUI honors `session.restore` for saved sheet, cursor, panel, and zoom state,
and bounds persisted history with the live `session.recent_files` value.
Unavailable external-link refresh, Hyprland workspace binding, global non-XLSX
defaults, and non-default CSV policies are rejected with actionable
configuration errors instead of being silently accepted.

The PR #193 review correction puts those decisions at their owning boundaries.
`apply_restored_session` gates both launch and file-open restoration, while
session persistence keeps disabled view keys intact and updates recent history
through one `SessionState` operation. The recent-file maximum is exported by
`omacell-conf` and shared with UI persistence. Reserved strings and booleans use
table-driven validators that retain distinct schema-illegal and unavailable
diagnostics. `FileSession` now owns the single XLSX retention snapshot and
post-save discard policy used by save/export, and its end-to-end regression
lives in a dedicated CLI integration test rather than the large adapter module.

Key files:

- `crates/conf/src/{lib,schema,layer,theme,font,watch,setup,keys,paths,error}.rs`
- `default/config.toml`, `default/themed/omacell.toml.tpl`, `default/keys/{classic,modal}.toml`
- `docs/schemas/config.schema.json`
- Fixtures: `tests/fixtures/omarchy-themes/` (22 stock + 3 community)
- `scripts/fetch-omarchy-themes.sh`
- Tests: `layering`, `theme`, `font`, `watch`, `setup`, `migration`, `schema_snapshot`
- Benchmark: `crates/conf/benches/theme_reload.rs` plus a committed static Omarchy fixture

## Interfaces exposed (for dependents)

| Item | Notes |
|---|---|
| `Config`, `package_defaults` | Appendix B |
| `load` / `load_with_env` / `load_with_options` / `LoadedConfig::explain` | validated layers + provenance; `LoadOptions` retains explicit config/workbook/env/CLI/theme inputs across reloads |
| `workbook_settings_overlay` | canonical frozen `WorkbookSettings` → config layer mapping for every frontend |
| `Migration`, `reset_user_file` | schema backup/rewrite evidence and safe timestamped reset backup |
| `ThemeRoles`, `resolve_roles`, `resolve_roles_with_override`, `mix`, `ColorsToml` | §7.1 including rendered/user/explicit overlays and array palettes |
| `ConfigStore`, `ReloadEvent` | last-good live reload; watches config/keymap trees, explicit config/theme, active theme, shell, and font paths when enabled |
| `Paths::from_env` | absolute XDG config/state roots with HOME fallbacks; relative XDG values are ignored |
| `setup_omarchy`, `uninstall_omarchy`, `SetupReport`, `UninstallReport`, `HYPRLAND_SNIPPET` | §7.6; conservative owned-file lifecycle; never writes `/usr/share/omarchy` |
| `keys::check_hyprland` | §7.4 |
| `font::ShellTokens`, `shell_tokens`, `substitute_file_font`, `resolve_family`, `resolve_font_path` | §7.2; active/user shell overlay, loadable fontconfig path, spacing and corners |
| `docs/schemas/config.schema.json` | schemars |

No commands. `omacell config` / `omacell setup omarchy` are WP-13.

## Deviations from the spec or the package (with reasons)

- **`[config] live_reload` / `debounce_ms`** are extra keys so the library can watch without a CLI.
- **`ReloadEvent` lives in `conf`**, not frozen `core::Event` (non_exhaustive but G0-frozen). WP-13 can map it.
- **SIGUSR1** is `ConfigStore::reload`; the binary binds the signal (no `unsafe` in this crate).
- **Portal color-scheme** is best-effort zbus; tests inject light/dark via `resolve_roles(..., portal_light)`.
- **Keymaps** contain Appendix A chords. WP-14 completed the shared keymap vocabulary and later integration removed every owned command from the deferred table.
- **`cargo deny` ignores RUSTSEC-2026-0192** (`ttf-parser` unmaintained via `fontdb` 0.23, pre-approved).
- **The no-flicker/no-lost-edit-state portion of the theme reload gate** belongs to the WP-16 renderer. This package records the full configuration/theme re-derivation time and preserves all non-file layers during reload.
- **Several frozen file/session options remain reserved but unavailable.** Their
  schema fields and intended enum values are retained for compatibility, while
  validation rejects unsupported opt-ins and non-default policies. Explicit
  destination extensions and reviewed CSV import plans remain the supported
  per-operation controls.

## Measurements

- 2026-09-04 focused validation: `cargo test -p omacell-conf` — 64 total
  checks: 5 unit tests; layering 16, theme 13 (including malformed ambient and
  Unicode input), font 4, watch 10, setup/uninstall 9, migration 4, schema
  snapshot 2; and one doc-test.
- Criterion `theme_reload/full_config_and_theme --quick --save-baseline wp12-handoff` — **12.112 ms** midpoint, including config parsing/validation, active/rendered/user theme resolution, contrast checks, shell tokens, and loadable fontconfig resolution. This is about 8.3× inside the spec's **< 100 ms** theme reload gate.
- `just check` and `cargo deny check` — pass after review fixes.
- 2026-09-04 hardening gate: strict package Clippy, `cargo deny check`, and
  `cargo audit` pass. The exact `just check` recipe passes, including formatting,
  workspace all-target Clippy, all workspace tests/doctests, and warning-free
  docs. The workstation run used two build jobs and linker non-mmap output after
  clearing rebuildable incremental artifacts from the dedicated target cache.
- 2026-09-02 environment-filter follow-up: the complete `omacell-conf` suite
  passes, including a mixed environment regression that proves two unrelated
  process controls are ignored while a recognized override applies with exact
  provenance. The exact unsandboxed `just check` gate also passes, including
  workspace-wide strict Clippy, Unix-socket IPC, real LibreOffice conversions,
  and warning-free documentation.
- 2026-09-06 red phase: the three initial configuration-validation regressions,
  both GUI session-consumption regressions, and XLSX preservation regression all
  failed against the merged baseline. The adjacent unsupported file-default
  regression also failed before validation was added.
- 2026-09-06 focused green phase: all four new configuration validation tests,
  the XLSX preservation/discard-and-reload regression, and all three
  session-named GUI lifecycle tests pass. Strict all-target Clippy passes for
  `omacell-conf`, `omacell-ui`, `omacell-cli`, and `omacell-gui`; generated
  documentation and schema snapshots are current.
- 2026-09-06 full gate: the exact unsandboxed `just check` recipe passes,
  including formatting, workspace-wide all-target Clippy, all workspace tests
  and doctests, Unix-socket and loopback integrations, LibreOffice round trips,
  and warning-free documentation.
- 2026-09-07 review red phase: the extended disabled-restore lifecycle test
  failed after `file.open` because the wiped session snapshot replaced the
  workbook zoom (`1.0` instead of `1.5`). The same regression checks that a
  disabled-restore run preserves saved view keys on disk while updating recent
  files.
- 2026-09-07 review green phase: the extended GUI lifecycle regression, shared
  UI persistence suite, complete configuration suite, CLI file lifecycle suite,
  and standalone XLSX preservation integration test pass. Strict all-target
  Clippy passes for all affected packages. The exact unsandboxed `just check`
  recipe passes again, including all workspace tests/doctests, Unix sockets,
  loopback integrations, LibreOffice round trips, and warning-free docs.

## Open questions / decisions needed

1. **Resolved:** SIGUSR1 is bound only at the WP-13 composition root.
2. **Resolved:** WP-14 owns the engine while shipped `default/keys/*.toml` data
   remains the base layer extended by user overrides.

## RFC (only if a frozen contract changed)

None. `core::Event` is unchanged.

## Checklist

- [x] `just check` green locally
- [x] Every acceptance criterion ticked with evidence
- [x] Docs warning-free; public items documented
- [x] Baselines recorded (`theme_reload/full_config_and_theme`: 12.112 ms; Criterion baseline `wp12-handoff`)
- [x] No new `TODO(` without a `WP-` reference; new deps `toml`, `notify`, `fontdb`, `zbus`, `tracing` are pre-approved
- [x] Setup, uninstall, and migration writes stay inside the supplied home/config/state paths; tests use managed temporary directories

### Acceptance

- [x] Layering precedence for package/user/workbook/env/cli; explicit config files and canonical workbook overlays; unknown file/CLI keys and invalid recognized environment values fail, while unrelated `OMACELL_*` process controls are ignored; `explain` names the layer; reload retains every source
- [x] Template-vs-code equality across every fixture; rendered/user/explicit overlay precedence; exact array palettes; contrast runs after overlays; light-mode mix is darker than background
- [x] Invalid intermediate write keeps last good and emits `ReloadEvent::Invalid`; an unavailable watcher also keeps the loaded config and reports the disabled watcher; `live_reload = false` starts no watcher; active-theme changes emit `ThemeChanged`; user/default-profile keymaps and external config/theme files emit reload events
- [x] Migration advances only the root schema value while preserving comments, formatting, symlinks, and target permissions
- [x] `setup omarchy` creates exactly the template, no-instance-safe hook, and five skill links in a temp home; menu only when confirmed; reformatted rows do not duplicate; comments and unrelated rows are preserved
- [x] `uninstall_omarchy` removes only unchanged owned files, exact owned links, and exact menu commands while retaining modified and unrelated content
- [x] File/session configuration is either consumed or rejected explicitly:
  XLSX opaque-part preservation, saved-view restore, and recent-file limits have
  production regressions; reserved external-link, workspace, global format, and
  CSV defaults fail validation when enabled or changed
- [x] PR #193 review feedback is resolved at owning abstractions: restore policy
  covers launch, file-open, and persistence; recent history has one shared cap;
  reserved validation is table-driven; XLSX retention belongs to `FileSession`;
  the large implementation modules no longer contain the preservation scenario
